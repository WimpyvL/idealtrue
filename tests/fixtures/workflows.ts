export const fixedNow = '2026-04-24T08:00:00.000Z';

export const workflowUsers = {
  guest: {
    id: 'user-guest-verified',
    email: 'guest@example.com',
    emailVerified: true,
    displayName: 'Guest Verified',
    photoUrl: '',
    role: 'guest',
    hostPlan: 'standard',
    kycStatus: 'verified',
    accountStatus: 'active',
    accountStatusReason: null,
    balance: 0,
    referralCount: 1,
    tier: 'bronze',
    referralCode: 'GUEST1',
    referredByCode: null,
    paymentMethod: null,
    paymentInstructions: null,
    paymentReferencePrefix: null,
    createdAt: '2026-04-01T10:00:00.000Z',
    updatedAt: fixedNow,
  },
  host: {
    id: 'user-host-verified',
    email: 'host@example.com',
    emailVerified: true,
    displayName: 'Host Verified',
    photoUrl: '',
    role: 'host',
    hostPlan: 'professional',
    kycStatus: 'verified',
    accountStatus: 'active',
    accountStatusReason: null,
    balance: 0,
    referralCount: 3,
    tier: 'silver',
    referralCode: 'HOST1',
    referredByCode: null,
    paymentMethod: 'bank_transfer',
    paymentInstructions: 'Pay into the host trust account.',
    paymentReferencePrefix: 'HOST',
    createdAt: '2026-04-01T10:00:00.000Z',
    updatedAt: fixedNow,
  },
  admin: {
    id: 'user-admin',
    email: 'admin@example.com',
    emailVerified: true,
    displayName: 'Admin User',
    photoUrl: '',
    role: 'admin',
    hostPlan: 'premium',
    kycStatus: 'verified',
    accountStatus: 'active',
    accountStatusReason: null,
    balance: 0,
    referralCount: 0,
    tier: 'gold',
    referralCode: 'ADMIN1',
    referredByCode: null,
    paymentMethod: null,
    paymentInstructions: null,
    paymentReferencePrefix: null,
    createdAt: '2026-04-01T10:00:00.000Z',
    updatedAt: fixedNow,
  },
  suspended: {
    id: 'user-suspended',
    email: 'suspended@example.com',
    emailVerified: true,
    displayName: 'Suspended User',
    photoUrl: '',
    role: 'guest',
    hostPlan: 'standard',
    kycStatus: 'none',
    accountStatus: 'suspended',
    accountStatusReason: 'Policy review is open.',
    balance: 0,
    referralCount: 0,
    tier: 'bronze',
    referralCode: 'SUSP1',
    referredByCode: null,
    paymentMethod: null,
    paymentInstructions: null,
    paymentReferencePrefix: null,
    createdAt: '2026-04-01T10:00:00.000Z',
    updatedAt: fixedNow,
  },
  greylistedHost: {
    id: 'user-host-greylisted',
    email: 'greylisted-host@example.com',
    emailVerified: true,
    displayName: 'Greylisted Host',
    photoUrl: '',
    role: 'host',
    hostPlan: 'standard',
    kycStatus: 'verified',
    accountStatus: 'active',
    accountStatusReason: null,
    balance: 0,
    referralCount: 0,
    tier: 'bronze',
    referralCode: 'GREY1',
    referredByCode: null,
    paymentMethod: 'bank_transfer',
    paymentInstructions: 'Payment instructions unavailable.',
    paymentReferencePrefix: 'GREY',
    createdAt: '2026-04-01T10:00:00.000Z',
    updatedAt: fixedNow,
  },
  kycPendingHost: {
    id: 'user-host-kyc-pending',
    email: 'pending-host@example.com',
    emailVerified: true,
    displayName: 'KYC Pending Host',
    photoUrl: '',
    role: 'host',
    hostPlan: 'standard',
    kycStatus: 'pending',
    accountStatus: 'active',
    accountStatusReason: null,
    balance: 0,
    referralCount: 0,
    tier: 'bronze',
    referralCode: 'PEND1',
    referredByCode: null,
    paymentMethod: null,
    paymentInstructions: null,
    paymentReferencePrefix: null,
    createdAt: '2026-04-01T10:00:00.000Z',
    updatedAt: fixedNow,
  },
  kycRejectedHost: {
    id: 'user-host-kyc-rejected',
    email: 'rejected-host@example.com',
    emailVerified: true,
    displayName: 'KYC Rejected Host',
    photoUrl: '',
    role: 'host',
    hostPlan: 'standard',
    kycStatus: 'rejected',
    accountStatus: 'active',
    accountStatusReason: null,
    balance: 0,
    referralCount: 0,
    tier: 'bronze',
    referralCode: 'REJ1',
    referredByCode: null,
    paymentMethod: null,
    paymentInstructions: null,
    paymentReferencePrefix: null,
    createdAt: '2026-04-01T10:00:00.000Z',
    updatedAt: fixedNow,
  },
};

export const workflowListings = {
  active: {
    id: 'listing-active-cape-town',
    hostId: workflowUsers.host.id,
    title: 'Sea Point Stay',
    description: 'Ocean-facing apartment',
    location: 'Cape Town',
    area: 'Sea Point',
    province: 'Western Cape',
    category: 'apartment',
    type: 'apartment',
    pricePerNight: 1800,
    discountPercent: 10,
    breakageDeposit: 750,
    adults: 2,
    children: 1,
    bedrooms: 1,
    bathrooms: 1,
    amenities: ['wifi'],
    facilities: ['parking'],
    restaurantOffers: [],
    images: [],
    videoUrl: null,
    isSelfCatering: true,
    hasRestaurant: false,
    isOccupied: false,
    latitude: -33.9,
    longitude: 18.4,
    blockedDates: [],
    availabilityBlocks: [],
    status: 'active',
    createdAt: '2026-04-01T10:00:00.000Z',
    updatedAt: fixedNow,
  },
  greylistedHost: {
    id: 'listing-greylisted-host',
    hostId: workflowUsers.greylistedHost.id,
    title: 'Hidden Billing Risk Stay',
    description: 'Should not appear publicly while greylisted.',
    location: 'Durban',
    area: 'Umhlanga',
    province: 'KwaZulu-Natal',
    category: 'apartment',
    type: 'apartment',
    pricePerNight: 1200,
    discountPercent: 0,
    breakageDeposit: 500,
    adults: 2,
    children: 0,
    bedrooms: 1,
    bathrooms: 1,
    amenities: ['wifi'],
    facilities: [],
    restaurantOffers: [],
    images: [],
    videoUrl: null,
    isSelfCatering: true,
    hasRestaurant: false,
    isOccupied: false,
    latitude: -29.7,
    longitude: 31.0,
    blockedDates: [],
    availabilityBlocks: [],
    status: 'active',
    createdAt: '2026-04-01T10:00:00.000Z',
    updatedAt: fixedNow,
  },
  blocked: {
    id: 'listing-manual-blocks',
    hostId: workflowUsers.host.id,
    title: 'Garden Route Blocked Stay',
    description: 'Listing with manual and booking-derived availability blocks.',
    location: 'Knysna',
    area: 'Leisure Isle',
    province: 'Western Cape',
    category: 'house',
    type: 'house',
    pricePerNight: 2200,
    discountPercent: 0,
    breakageDeposit: 1000,
    adults: 4,
    children: 2,
    bedrooms: 3,
    bathrooms: 2,
    amenities: ['wifi'],
    facilities: ['pool'],
    restaurantOffers: [],
    images: [],
    videoUrl: null,
    isSelfCatering: true,
    hasRestaurant: false,
    isOccupied: false,
    latitude: -34.0,
    longitude: 23.0,
    blockedDates: ['2026-05-10'],
    availabilityBlocks: [
      {
        id: 'block-manual-1',
        listingId: 'listing-manual-blocks',
        source: 'MANUAL',
        startDate: '2026-05-10',
        endDate: '2026-05-11',
        note: 'Owner maintenance',
      },
      {
        id: 'block-approved-1',
        listingId: 'listing-manual-blocks',
        source: 'APPROVED_HOLD',
        startDate: '2026-05-15',
        endDate: '2026-05-18',
        note: null,
      },
      {
        id: 'block-booked-1',
        listingId: 'listing-manual-blocks',
        source: 'BOOKED',
        startDate: '2026-05-20',
        endDate: '2026-05-23',
        note: null,
      },
    ],
    status: 'active',
    createdAt: '2026-04-01T10:00:00.000Z',
    updatedAt: fixedNow,
  },
};

export const workflowBookings = {
  pending: {
    id: 'booking-pending',
    listingId: workflowListings.active.id,
    guestId: workflowUsers.guest.id,
    hostId: workflowUsers.host.id,
    checkIn: '2026-05-01T00:00:00.000Z',
    checkOut: '2026-05-04T00:00:00.000Z',
    adults: 1,
    children: 0,
    totalPrice: 4860,
    breakageDeposit: 750,
    inquiryState: 'PENDING',
    paymentState: 'UNPAID',
    declineReason: null,
    paymentMethod: 'bank_transfer',
    paymentInstructions: 'Pay within 24 hours after approval.',
    paymentReference: null,
    paymentProofUrl: null,
    createdAt: '2026-04-24T07:00:00.000Z',
    updatedAt: '2026-04-24T07:00:00.000Z',
  },
  approvedAwaitingPayment: {
    id: 'booking-approved-awaiting-payment',
    listingId: workflowListings.active.id,
    guestId: workflowUsers.guest.id,
    hostId: workflowUsers.host.id,
    checkIn: '2026-05-05T00:00:00.000Z',
    checkOut: '2026-05-08T00:00:00.000Z',
    adults: 1,
    children: 0,
    totalPrice: 4860,
    breakageDeposit: 750,
    inquiryState: 'APPROVED',
    paymentState: 'INITIATED',
    declineReason: null,
    paymentMethod: 'bank_transfer',
    paymentInstructions: 'Pay within 24 hours.',
    paymentReference: 'HOST-booking-approved-awaiting-payment',
    paymentProofUrl: null,
    approvedAt: '2026-04-24T07:30:00.000Z',
    paymentDueAt: '2026-04-25T07:30:00.000Z',
    createdAt: '2026-04-24T07:00:00.000Z',
    updatedAt: '2026-04-24T07:30:00.000Z',
  },
  proofSubmitted: {
    id: 'booking-proof-submitted',
    listingId: workflowListings.active.id,
    guestId: workflowUsers.guest.id,
    hostId: workflowUsers.host.id,
    checkIn: '2026-05-09T00:00:00.000Z',
    checkOut: '2026-05-12T00:00:00.000Z',
    adults: 1,
    children: 0,
    totalPrice: 4860,
    breakageDeposit: 750,
    inquiryState: 'APPROVED',
    paymentState: 'PROOF_SUBMITTED',
    declineReason: null,
    paymentMethod: 'bank_transfer',
    paymentInstructions: 'Pay within 24 hours.',
    paymentReference: 'HOST-booking-proof-submitted',
    paymentProofUrl: 'https://storage.example/proof.jpg',
    paymentProofSubmittedAt: '2026-04-24T07:45:00.000Z',
    createdAt: '2026-04-24T07:00:00.000Z',
    updatedAt: '2026-04-24T07:45:00.000Z',
  },
  confirmed: {
    id: 'booking-confirmed',
    listingId: workflowListings.active.id,
    guestId: workflowUsers.guest.id,
    hostId: workflowUsers.host.id,
    checkIn: '2026-05-13T00:00:00.000Z',
    checkOut: '2026-05-16T00:00:00.000Z',
    adults: 1,
    children: 0,
    totalPrice: 4860,
    breakageDeposit: 750,
    inquiryState: 'CONFIRMED',
    paymentState: 'CONFIRMED',
    declineReason: null,
    paymentMethod: 'bank_transfer',
    paymentInstructions: 'Paid.',
    paymentReference: 'HOST-booking-confirmed',
    paymentProofUrl: 'https://storage.example/proof-confirmed.jpg',
    confirmedAt: '2026-04-24T08:00:00.000Z',
    createdAt: '2026-04-24T07:00:00.000Z',
    updatedAt: fixedNow,
  },
  declined: {
    id: 'booking-declined',
    listingId: workflowListings.active.id,
    guestId: workflowUsers.guest.id,
    hostId: workflowUsers.host.id,
    checkIn: '2026-05-17T00:00:00.000Z',
    checkOut: '2026-05-20T00:00:00.000Z',
    adults: 1,
    children: 0,
    totalPrice: 4860,
    breakageDeposit: 750,
    inquiryState: 'DECLINED',
    paymentState: 'UNPAID',
    declineReason: 'Dates no longer available.',
    paymentMethod: 'bank_transfer',
    paymentInstructions: null,
    paymentReference: null,
    paymentProofUrl: null,
    createdAt: '2026-04-24T07:00:00.000Z',
    updatedAt: fixedNow,
  },
  expired: {
    id: 'booking-expired',
    listingId: workflowListings.active.id,
    guestId: workflowUsers.guest.id,
    hostId: workflowUsers.host.id,
    checkIn: '2026-05-21T00:00:00.000Z',
    checkOut: '2026-05-24T00:00:00.000Z',
    adults: 1,
    children: 0,
    totalPrice: 4860,
    breakageDeposit: 750,
    inquiryState: 'EXPIRED',
    paymentState: 'UNPAID',
    declineReason: null,
    paymentMethod: 'bank_transfer',
    paymentInstructions: null,
    paymentReference: null,
    paymentProofUrl: null,
    expiresAt: '2026-04-23T07:00:00.000Z',
    createdAt: '2026-04-21T07:00:00.000Z',
    updatedAt: fixedNow,
  },
};

export const workflowBilling = {
  voucherActive: {
    userId: workflowUsers.host.id,
    status: 'active',
    source: 'voucher',
    plan: 'professional',
    voucherCode: 'HOST-ABC1234567',
    cycleStartedAt: '2026-04-01T00:00:00.000Z',
    cycleEndsAt: '2026-07-01T00:00:00.000Z',
    reminderStartsAt: '2026-06-24T00:00:00.000Z',
    cardOnFile: false,
    greylistedAt: null,
    updatedAt: fixedNow,
  },
  greylistedOverdue: {
    userId: workflowUsers.greylistedHost.id,
    status: 'greylisted',
    source: 'voucher',
    plan: 'standard',
    voucherCode: 'HOST-OVERDUE1',
    cycleStartedAt: '2026-01-01T00:00:00.000Z',
    cycleEndsAt: '2026-04-01T00:00:00.000Z',
    reminderStartsAt: '2026-03-25T00:00:00.000Z',
    cardOnFile: false,
    greylistedAt: '2026-04-08T00:00:00.000Z',
    updatedAt: fixedNow,
  },
  checkoutPaid: {
    checkoutId: 'checkout-paid-subscription',
    checkoutType: 'subscription',
    status: 'paid',
  },
  checkoutFailed: {
    checkoutId: 'checkout-failed-content',
    checkoutType: 'content_credits',
    status: 'failed',
  },
};

export const workflowKyc = {
  pending: {
    id: 'kyc-pending',
    userId: workflowUsers.kycPendingHost.id,
    idType: 'id_card',
    idNumber: '9001015009087',
    idImageKey: 'kyc/user-host-kyc-pending/id.jpg',
    selfieImageKey: 'kyc/user-host-kyc-pending/selfie.jpg',
    status: 'pending',
    rejectionReason: null,
    submittedAt: '2026-04-24T07:00:00.000Z',
    reviewedAt: null,
    reviewerId: null,
  },
  rejected: {
    id: 'kyc-rejected',
    userId: workflowUsers.kycRejectedHost.id,
    idType: 'passport',
    idNumber: 'A12345678',
    idImageKey: 'kyc/user-host-kyc-rejected/id.jpg',
    selfieImageKey: 'kyc/user-host-kyc-rejected/selfie.jpg',
    status: 'rejected',
    rejectionReason: 'Document image was unreadable.',
    submittedAt: '2026-04-23T07:00:00.000Z',
    reviewedAt: fixedNow,
    reviewerId: workflowUsers.admin.id,
  },
};

export const workflowNotifications = {
  unreadBooking: {
    id: 'notification-booking-payment',
    title: 'Payment requested',
    message: 'Your booking was approved. Submit payment to confirm it.',
    type: 'info',
    target: workflowUsers.guest.id,
    actionPath: '/guest',
    createdAt: fixedNow,
    readAt: null,
    dismissedAt: null,
  },
  readReferral: {
    id: 'notification-referral-read',
    title: 'Referral reward approved',
    message: 'Your referral reward is now visible.',
    type: 'success',
    target: workflowUsers.host.id,
    actionPath: '/referral',
    createdAt: fixedNow,
    readAt: fixedNow,
    dismissedAt: null,
  },
  dismissedAdmin: {
    id: 'notification-admin-dismissed',
    title: 'Platform update',
    message: 'Admin broadcast dismissed by user.',
    type: 'info',
    target: 'all',
    actionPath: '/account',
    createdAt: fixedNow,
    readAt: fixedNow,
    dismissedAt: fixedNow,
  },
};

export const workflowReferrals = {
  approved: {
    id: 'referral-approved',
    referrerId: workflowUsers.host.id,
    referredUserId: workflowUsers.guest.id,
    amount: 100,
    status: 'approved',
    createdAt: '2026-04-20T10:00:00.000Z',
    updatedAt: fixedNow,
  },
  rejected: {
    id: 'referral-rejected',
    referrerId: workflowUsers.guest.id,
    referredUserId: workflowUsers.suspended.id,
    amount: 0,
    status: 'rejected',
    createdAt: '2026-04-20T10:00:00.000Z',
    updatedAt: fixedNow,
  },
};

export const workflowMessages = {
  guestMessage: {
    id: 'message-guest',
    bookingId: workflowBookings.confirmed.id,
    senderId: workflowUsers.guest.id,
    receiverId: workflowUsers.host.id,
    text: 'Hi, what time is check-in?',
    isSystem: false,
    suggestionType: null,
    attachmentUrl: null,
    createdAt: fixedNow,
  },
  attachmentMessage: {
    id: 'message-attachment',
    bookingId: workflowBookings.confirmed.id,
    senderId: workflowUsers.host.id,
    receiverId: workflowUsers.guest.id,
    text: 'Attached the access instructions.',
    isSystem: false,
    suggestionType: null,
    attachmentUrl: 'https://storage.example/chat/access.pdf',
    createdAt: fixedNow,
  },
};

export const workflowReviews = {
  pending: {
    id: 'review-pending',
    bookingId: workflowBookings.confirmed.id,
    listingId: workflowListings.active.id,
    guestId: workflowUsers.guest.id,
    rating: 5,
    comment: 'Great stay and clear host communication.',
    status: 'pending',
    createdAt: fixedNow,
    updatedAt: fixedNow,
  },
  approved: {
    id: 'review-approved',
    bookingId: workflowBookings.confirmed.id,
    listingId: workflowListings.active.id,
    guestId: workflowUsers.guest.id,
    rating: 5,
    comment: 'Approved public review.',
    status: 'approved',
    createdAt: fixedNow,
    updatedAt: fixedNow,
  },
};

export const workflowContentDrafts = {
  draft: {
    id: 'content-draft-1',
    userId: workflowUsers.host.id,
    listingId: workflowListings.active.id,
    listingTitle: workflowListings.active.title,
    listingLocation: workflowListings.active.location,
    platform: 'instagram',
    tone: 'warm',
    templateId: 'stay_carousel',
    templateName: 'Stay Carousel',
    status: 'draft',
    content: 'Book a long weekend at Sea Point Stay.',
    scheduledFor: null,
    publishedAt: null,
    createdAt: fixedNow,
    updatedAt: fixedNow,
  },
  scheduled: {
    id: 'content-draft-scheduled',
    userId: workflowUsers.host.id,
    listingId: workflowListings.active.id,
    listingTitle: workflowListings.active.title,
    listingLocation: workflowListings.active.location,
    platform: 'facebook',
    tone: 'luxury',
    templateId: 'story_pack',
    templateName: 'Story Pack',
    status: 'scheduled',
    content: 'Scheduled coastal stay creative.',
    scheduledFor: '2026-04-30T08:00:00.000Z',
    publishedAt: null,
    createdAt: fixedNow,
    updatedAt: fixedNow,
  },
};

export const workflowCoverageMatrix = [
  {
    workflow: 'Auth and account lifecycle',
    ownerArea: ['identity'],
    entryPoints: ['/signup', '/account', '/guest', '/host', '/admin'],
    backendServices: ['identity'],
    expectedOutcome: 'Users can authenticate, recover access, update account state, and be restricted safely.',
    happyPath: 'signup -> verify email -> signin -> update profile',
    failurePath: 'invalid credentials, invalid token, suspended account, and upload failure are rejected',
    roleRules: 'anonymous auth routes, self-service profile updates, admin-only account moderation',
    asyncEffects: ['verification email', 'password reset email', 'restricted account listing pause'],
    notificationEffects: ['admin status notifications when moderation creates them'],
    externalBoundaries: ['email provider', 'profile photo bucket', 'HttpOnly session cookie'],
    fixtureKeys: ['guest', 'host', 'admin', 'suspended'],
    currentCoverage: ['tests/ui/auth-context.test.tsx', 'tests/session-cookie.test.ts', 'tests/dev-login-env.test.ts', 'tests/account-status.test.ts', 'tests/api-clients.test.ts', 'tests/workflow-client-contracts.test.ts', 'tests/e2e/auth-account.spec.ts'],
    requiredNextCoverage: ['backend auth token expiry tests', 'profile photo upload UI failure tests'],
  },
  {
    workflow: 'Listing discovery and booking request',
    ownerArea: ['catalog', 'booking'],
    entryPoints: ['/', '/guest'],
    backendServices: ['catalog', 'booking', 'reviews'],
    expectedOutcome: 'Guests find available listings and submit valid booking requests.',
    happyPath: 'guest discovers active listing and creates pending enquiry',
    failurePath: 'unavailable dates, hidden listings, unauthenticated requests, and invalid ranges are rejected',
    roleRules: 'public listing reads, authenticated guest booking writes, backend-owned availability validation',
    asyncEffects: ['pending enquiry creation'],
    notificationEffects: ['host enquiry notification'],
    externalBoundaries: ['map and image assets'],
    fixtureKeys: ['active listing', 'pending booking'],
    currentCoverage: ['tests/listing-availability-ui.test.ts', 'tests/catalog-availability.test.ts', 'tests/e2e/happy-path.spec.ts'],
    requiredNextCoverage: ['tests/e2e/listing-discovery.spec.ts', 'backend createBooking rejection tests'],
  },
  {
    workflow: 'Booking, enquiry, and payment lifecycle',
    ownerArea: ['booking', 'catalog', 'ops'],
    entryPoints: ['/guest', '/host/enquiries', '/host'],
    backendServices: ['booking', 'catalog', 'ops'],
    expectedOutcome: 'Bookings move through enquiry, approval, proof, confirmation, expiry, and review states.',
    happyPath: 'guest request -> host approve -> guest proof -> host confirm -> guest review',
    failurePath: 'invalid transitions, expired holds, payment proof in wrong state, and availability conflicts are rejected',
    roleRules: 'guest owns request/proof/review, host owns approve/decline/confirm for own listing',
    asyncEffects: ['inquiry expiry cycle', 'availability hold writes and releases'],
    notificationEffects: ['booking status and payment notifications'],
    externalBoundaries: ['off-platform payment proof'],
    fixtureKeys: ['pending booking', 'approved booking', 'proof booking', 'confirmed booking', 'declined booking', 'expired booking'],
    currentCoverage: ['tests/booking-workflow.test.ts', 'tests/inquiry-state.test.ts', 'tests/ui/guest-dashboard.test.tsx', 'tests/ui/host-dashboard.test.tsx', 'tests/api-clients.test.ts', 'tests/e2e/happy-path.spec.ts'],
    requiredNextCoverage: ['tests/e2e/booking-payment-review.spec.ts', 'backend expiry mutation tests'],
  },
  {
    workflow: 'Host availability management',
    ownerArea: ['catalog'],
    entryPoints: ['/host/availability', '/host/listings'],
    backendServices: ['catalog', 'booking'],
    expectedOutcome: 'Hosts manage manual blocks while booking-owned locked dates remain protected.',
    happyPath: 'host blocks and unblocks a manual range',
    failurePath: 'locked approved/confirmed dates and non-owner updates are rejected',
    roleRules: 'host-only mutation of own listing availability',
    asyncEffects: ['availability summary refresh'],
    notificationEffects: [],
    externalBoundaries: [],
    fixtureKeys: ['blocked listing'],
    currentCoverage: ['tests/catalog-availability.test.ts', 'tests/host-availability.test.ts', 'tests/listing-availability-ui.test.ts'],
    requiredNextCoverage: ['tests/e2e/host-availability.spec.ts', 'backend permission tests'],
  },
  {
    workflow: 'Listing creation, editing, quota checks, media upload',
    ownerArea: ['catalog', 'billing', 'ops'],
    entryPoints: ['/host/create-listing', '/host/edit-listing/:id', '/host/listings', '/pricing'],
    backendServices: ['catalog', 'billing', 'ops'],
    expectedOutcome: 'Hosts create and maintain listings within KYC, billing, quota, and media rules.',
    happyPath: 'verified host creates and edits listing with allowed media',
    failurePath: 'KYC missing, quota exceeded, greylisted host, media limit, and upload failure are rejected',
    roleRules: 'host owns own listings, admin moderates listing state',
    asyncEffects: ['signed media upload'],
    notificationEffects: ['listing moderation notifications'],
    externalBoundaries: ['listing image bucket', 'listing video bucket', 'direct upload CORS'],
    fixtureKeys: ['verified host', 'greylisted host', 'active listing'],
    currentCoverage: ['tests/ui/create-listing.test.tsx', 'tests/catalog-billing-helpers.test.ts', 'tests/api-clients.test.ts'],
    requiredNextCoverage: ['tests/e2e/listing-management.spec.ts', 'backend signed upload permission tests'],
  },
  {
    workflow: 'KYC submission and admin review',
    ownerArea: ['ops', 'identity'],
    entryPoints: ['/account', '/host/create-listing', '/admin'],
    backendServices: ['ops', 'identity'],
    expectedOutcome: 'Hosts submit KYC evidence and admins approve or reject it with capability changes.',
    happyPath: 'host submits KYC, admin previews assets, admin approves',
    failurePath: 'missing docs, preview failure, duplicate pending submission, and non-admin review are rejected',
    roleRules: 'user submits own KYC, admin reviews all submissions',
    asyncEffects: ['secure asset preview generation', 'identity status sync'],
    notificationEffects: ['KYC approval and rejection notifications'],
    externalBoundaries: ['KYC docs bucket', 'secure preview URLs'],
    fixtureKeys: ['KYC pending host', 'KYC rejected host'],
    currentCoverage: ['tests/api-clients.test.ts'],
    requiredNextCoverage: ['tests/e2e/kyc-admin-review.spec.ts', 'backend KYC permission tests'],
  },
  {
    workflow: 'Subscription checkout, voucher redemption, card save, greylisting, webhook completion',
    ownerArea: ['billing', 'catalog', 'identity', 'ops'],
    entryPoints: ['/pricing', '/host', '/admin'],
    backendServices: ['billing', 'catalog'],
    expectedOutcome: 'Host billing state drives entitlements, reminders, checkout completion, and greylist locks.',
    happyPath: 'host redeems voucher or checkout, saves card, and sees refreshed active billing state',
    failurePath: 'invalid voucher, failed checkout, overdue missing card, greylisted host operations, and failed webhook are handled',
    roleRules: 'host manages own billing, admin can inspect and greylist',
    asyncEffects: ['billing reminder cycle', 'webhook completion', 'checkout polling'],
    notificationEffects: ['billing reminder and greylist notifications'],
    externalBoundaries: ['Yoco checkout', 'Yoco webhooks', 'card tokenization'],
    fixtureKeys: ['voucher active billing', 'greylisted billing', 'paid checkout', 'failed checkout'],
    currentCoverage: ['tests/host-billing-lifecycle.test.ts', 'tests/host-billing-ui.test.ts', 'tests/catalog-billing-helpers.test.ts', 'tests/api-clients.test.ts'],
    requiredNextCoverage: ['tests/e2e/host-billing.spec.ts', 'backend webhook signature tests'],
  },
  {
    workflow: 'Content Studio draft generation, scheduling, publish-state tracking, credit top-up',
    ownerArea: ['billing', 'ai'],
    entryPoints: ['/host/social'],
    backendServices: ['billing'],
    expectedOutcome: 'Hosts generate, save, schedule, publish, and top up content drafts within credit limits.',
    happyPath: 'generate draft -> save -> schedule -> mark published',
    failurePath: 'no entitlement, insufficient credits, invalid schedule date, and provider failure are handled',
    roleRules: 'host owns own content drafts',
    asyncEffects: ['credit checkout completion', 'scheduled publish-state tracking'],
    notificationEffects: ['credit purchase or draft status notifications when enabled'],
    externalBoundaries: ['AI providers', 'future social APIs', 'content credit checkout'],
    fixtureKeys: ['draft content', 'scheduled content'],
    currentCoverage: ['tests/social-template-engine.test.ts', 'tests/ai-client.test.ts', 'tests/text-generation.test.ts', 'tests/workflow-client-contracts.test.ts'],
    requiredNextCoverage: ['tests/e2e/content-studio.spec.ts', 'backend draft credit debit tests'],
  },
  {
    workflow: 'Messaging and attachment upload',
    ownerArea: ['messaging', 'booking', 'ops'],
    entryPoints: ['/host/inbox', '/guest'],
    backendServices: ['messaging', 'booking', 'ops'],
    expectedOutcome: 'Booking parties exchange messages and attachments with counterparty notifications.',
    happyPath: 'guest and host send booking-scoped messages with optional attachment',
    failurePath: 'non-party access, missing booking, empty message, and attachment upload failure are rejected',
    roleRules: 'only booking guest and host can access booking messages',
    asyncEffects: ['attachment signed upload', 'message persistence'],
    notificationEffects: ['message notification to counterparty'],
    externalBoundaries: ['chat attachments bucket', 'direct upload CORS'],
    fixtureKeys: ['guest message', 'attachment message'],
    currentCoverage: ['tests/backend-notification-helpers.test.ts', 'tests/workflow-client-contracts.test.ts'],
    requiredNextCoverage: ['tests/e2e/messaging.spec.ts', 'tests/api-clients.test.ts', 'backend messaging permission tests'],
  },
  {
    workflow: 'Reviews',
    ownerArea: ['reviews', 'booking', 'ops'],
    entryPoints: ['/guest', '/', '/admin'],
    backendServices: ['reviews', 'booking'],
    expectedOutcome: 'Guests review completed stays and admins moderate public visibility.',
    happyPath: 'completed guest stay creates review and approved review appears publicly',
    failurePath: 'non-completed booking, duplicate review, invalid rating, and destructive delete are rejected or guarded',
    roleRules: 'guest reviews own completed booking, public reads approved reviews, admin moderates all reviews',
    asyncEffects: ['optional review summary generation'],
    notificationEffects: ['review submitted and moderated notifications'],
    externalBoundaries: ['AI review summary provider when used'],
    fixtureKeys: ['pending review', 'approved review'],
    currentCoverage: ['tests/api-clients.test.ts', 'tests/ai-client.test.ts', 'tests/workflow-client-contracts.test.ts'],
    requiredNextCoverage: ['tests/e2e/reviews.spec.ts', 'backend review eligibility tests'],
  },
  {
    workflow: 'Notifications',
    ownerArea: ['ops'],
    entryPoints: ['global notification provider', '/admin'],
    backendServices: ['ops'],
    expectedOutcome: 'Workflow notifications are delivered with persisted read and dismissed state.',
    happyPath: 'user receives, reads, and dismisses notification',
    failurePath: 'non-owner read/dismiss, invalid admin target, missing notification, and poll failure are handled',
    roleRules: 'users manage own notification state, admin manages admin notifications',
    asyncEffects: ['polling-backed delivery'],
    notificationEffects: ['this workflow owns notification delivery'],
    externalBoundaries: ['Encore proxy'],
    fixtureKeys: ['unread notification', 'read notification', 'dismissed notification'],
    currentCoverage: ['tests/ui/notification-context.test.tsx', 'tests/backend-notification-helpers.test.ts', 'tests/api-clients.test.ts'],
    requiredNextCoverage: ['tests/e2e/notifications.spec.ts', 'backend per-workflow notification tests'],
  },
  {
    workflow: 'Referrals and leaderboard',
    ownerArea: ['referrals', 'identity', 'ops'],
    entryPoints: ['/referral', '/host/referrals', '/account', '/admin'],
    backendServices: ['referrals', 'identity'],
    expectedOutcome: 'Referral signups, rewards, leaderboard, and admin manual rewards stay consistent.',
    happyPath: 'referral code signup creates visible reward and leaderboard state',
    failurePath: 'invalid code, self-referral, duplicate reward, rejected state, and missing delete target are handled',
    roleRules: 'users read own rewards and leaderboard, admin manages reward records',
    asyncEffects: ['reward creation during signup or admin action'],
    notificationEffects: ['referral reward notifications'],
    externalBoundaries: [],
    fixtureKeys: ['approved referral', 'rejected referral'],
    currentCoverage: ['tests/api-clients.test.ts', 'tests/backend-notification-helpers.test.ts', 'tests/workflow-client-contracts.test.ts'],
    requiredNextCoverage: ['tests/e2e/referrals.spec.ts', 'backend duplicate/self-referral tests'],
  },
  {
    workflow: 'Admin moderation and platform settings',
    ownerArea: ['ops', 'identity', 'catalog', 'booking', 'reviews', 'referrals', 'billing'],
    entryPoints: ['/admin'],
    backendServices: ['identity', 'catalog', 'booking', 'reviews', 'referrals', 'billing', 'ops'],
    expectedOutcome: 'Admins moderate users, listings, bookings, reviews, KYC, referrals, notifications, billing, and settings safely.',
    happyPath: 'admin reviews and updates moderation state across platform sections',
    failurePath: 'non-admin access, protected delete, validation failure, missing reason, and partial dashboard failure are handled',
    roleRules: 'admin-only route and admin-only endpoints',
    asyncEffects: ['audit log writes', 'observability reads'],
    notificationEffects: ['admin-created and moderation notifications'],
    externalBoundaries: ['KYC evidence previews', 'observability DB checks'],
    fixtureKeys: ['admin user', 'protected user', 'admin notification'],
    currentCoverage: ['tests/ui/admin-dashboard-data.test.tsx', 'tests/api-clients.test.ts', 'tests/account-status.test.ts'],
    requiredNextCoverage: ['tests/e2e/admin-moderation.spec.ts', 'backend destructive action tests'],
  },
  {
    workflow: 'Trip planner and AI-assisted planning',
    ownerArea: ['ai', 'frontend planning'],
    entryPoints: ['/planner'],
    backendServices: ['api/ai/*'],
    expectedOutcome: 'Guests receive bounded planning help with provider fallback and rate limiting.',
    happyPath: 'planner prompt returns bounded AI response',
    failurePath: 'empty prompt, oversized context, provider failure, and rate limit are handled',
    roleRules: 'route policy plus actor/IP-scoped rate limiting',
    asyncEffects: ['rate limiter state', 'provider fallback attempts'],
    notificationEffects: [],
    externalBoundaries: ['Gemini provider', 'DeepSeek provider', 'server AI endpoint proxy'],
    fixtureKeys: ['active listing', 'guest user'],
    currentCoverage: ['tests/ai-client.test.ts', 'tests/ai-rails.test.ts', 'tests/gemini-config.test.ts', 'tests/deepseek-config.test.ts', 'tests/text-generation.test.ts'],
    requiredNextCoverage: ['tests/e2e/trip-planner.spec.ts', 'UI planner error state tests'],
  },
] as const;
