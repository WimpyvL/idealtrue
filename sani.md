## Sani Notes

## Change Log

- 2026-06-08: Raised the local and CI quality bar beyond contract validation. Added ESLint with TypeScript and React Hooks rule coverage through `eslint.config.js`, replaced the old TypeScript-only lint script with `scripts/lint.mjs`, and folded the Encore backend typecheck into `npm run lint` so frontend and backend type drift are checked together. Added `scripts/verify.mjs` and `npm run verify` as the single developer/release gate: stale-file drift, lint/typechecks, full tests, production build, and bundle budget. Cleaned stale imports, unused variables, loose `any` usage in KYC/search surfaces, dead media helper code, and a useless payment-proof assignment caught by the new lint gate. Verification passed: `npm run verify`. Author signature: (|/) Klaasvaakie

- 2026-06-08: Hardened the app against frontend/backend contract drift. Added Zod runtime validation at the Encore client boundary for identity users, listings, availability summaries, bookings, reviews, referrals, notifications, platform settings, subscriptions, and admin checkout feeds before mapping into UI state. Added `tests/api-contract-validation.test.ts` to prove malformed session, listing, booking, and admin checkout payloads fail closed instead of silently corrupting dashboard/marketplace state. Added Knip as the stale-file drift guard with `npm run check:drift`, configured `knip.json` for the mixed Vercel/Encore repo shape, and wired it into `.github/workflows/staging-smoke.yml` with a workflow regression assertion. Verification passed: `npm run check:drift`, `npm run lint`, `npx tsc --noEmit -p encore/tsconfig.json`, `npx tsx --test tests/api-contract-validation.test.ts tests/staging-smoke-workflow.test.ts`, `npm run test:ui -- tests/ui/auth-context.test.tsx tests/ui/admin-dashboard-data.test.tsx`, `npm test`, and `npm run build`. Author signature: (|/) Klaasvaakie

- 2026-06-08: Audited admin dashboard data intake across the frontend loader, admin client, Encore endpoints, and section rendering. Confirmed the core admin loader requests users, listings, bookings, reviews, referrals, subscriptions, checkouts, host billing accounts, notifications, platform settings, KYC submissions, and observability. Found and fixed a real received-but-hidden data bug: Host Referrals were filtering for `booking` triggers even though host conversion rewards are created from paid `subscription` triggers, and manual host referrals were also being created as `signup` rewards. Updated `src/features/admin/dashboard-sections.tsx` so Host Referrals displays `subscription` rewards, updated `src/pages/AdminDashboard.tsx` so manual host referrals are created as subscription conversion rewards, expanded `tests/ui/admin-dashboard-data.test.tsx` so optional admin datasets must actually populate state, added `tests/ui/admin-referrals.test.tsx`, and added a source contract in `tests/admin-financials-contract.test.ts`. Verification passed: `npm run test:ui -- tests/ui/admin-referrals.test.tsx tests/ui/admin-dashboard-data.test.tsx`, `npx tsx --test tests/admin-financials-contract.test.ts`, `npm run lint`, `npm test`, and `npm run build`. Author signature: (|/) Klaasvaakie

- 2026-06-08: Ran a stale-code/drift audit after deployment. `knip` produced expected false positives for Vercel filesystem routes and Encore service entrypoints, but exposed three real app-owned leftovers: `src/data/mockListings.ts`, `src/pages/PlaceholderView.tsx`, and duplicate root `lib/utils.ts`. Removed those stale files. Also fixed stale payment documentation in `docs/payment-flow-diagrams.md` and `docs/diagrams/payment/06-fulfillment-by-purpose.mmd` so managed-hosting fulfilment now documents the required `identity.users.host_plan = premium` upgrade that unlocks listing quota; regenerated `docs/diagrams/payment/06-fulfillment-by-purpose.svg` and added a regression guard in `tests/workflow-coverage.test.ts`. Verification passed: `npx tsx --test tests/workflow-coverage.test.ts tests/admin-financials-contract.test.ts`, `npm run lint`, `npm run build`, and `npm test`. Author signature: (|/) Klaasvaakie

- 2026-06-08: Deployed commit `052db7e` everywhere. Pushed `main` to GitHub `origin`, which updates the frontend source path, pushed `main` to the Encore deploy remote for `ideal-stay-online-gh5i`, triggering staging deploy `1vegacmagip74obdoupg` and prod deploy `1vegacmagip74obdouq0`, and manually triggered Vercel production deploy `dpl_6zkVTDN5ReS7xnSdQjbpmRqMBae3` because the GitHub-triggered Vercel deployment was not visible yet. Vercel finished `Ready` and aliased `https://www.idealstay.co.za`. Live probes passed: homepage `200`, `/api/encore/auth/session` `401` for anonymous access, and `/api/encore/listings?status=active` `200`. Pre-deploy verification passed: `npm run lint`, `npm test`, `npx tsc --noEmit` in `encore`, and `npm run build`. Author signature: (|/) Klaasvaakie

- 2026-06-08: Fixed managed-hosting upgrade fulfilment so paid hosts can actually add more listings after purchase. Root cause: listing quota reads `users.host_plan`, and normal subscription fulfilment updated it, but managed-hosting fulfilment only set `management_mode = managed` plus the billing account plan. That left the host looking paid in billing while still `standard` for catalog quota, so the one-listing guard could keep blocking them. Updated `encore/billing/api.ts` so managed-hosting fulfilment also sets `users.host_plan = premium`. Added a regression contract in `tests/admin-financials-contract.test.ts` and rechecked the existing quota rules in `tests/catalog-billing-helpers.test.ts`. Verification passed: `npx tsx --test tests/admin-financials-contract.test.ts tests/catalog-billing-helpers.test.ts tests/payment-yoco-contracts.test.ts`, `npm run test:ui -- tests/ui/create-listing.test.tsx tests/ui/pricing-page.test.tsx`, `npm run lint`, and `npm run build`. Author signature: (|/) Klaasvaakie

- 2026-06-08: Fixed admin financials so real purchases made through the current standard billing payment flow show up in the admin dashboard. Root cause: `listAdminCheckouts` only read legacy `billing_checkout_sessions`, while current purchases are written to `billing_payment_intents`. Updated `encore/billing/api.ts` to union both sources into the existing admin checkout contract, with payment intents mapped by `purpose AS checkout_type`. Updated `src/features/admin/dashboard-sections.tsx` so Total Revenue is based on paid checkout purchases instead of subscriptions only, while Avg. Subscription still uses subscription revenue only. Added managed-hosting labels/details in the checkout table and regression coverage in `tests/admin-financials-contract.test.ts` plus `tests/ui/admin-financials.test.tsx`. Verification passed: `npm run test:ui -- tests/ui/admin-financials.test.tsx tests/ui/admin-dashboard-data.test.tsx`, `npx tsx --test tests/admin-financials-contract.test.ts tests/payment-yoco-contracts.test.ts`, `npm run lint`, and `npm run build`. Author signature: (|/) Klaasvaakie

- 2026-06-07: Fixed host enquiries so stale/passed cards do not keep pretending they are active. Added a `passed` host inquiry bucket in `src/lib/inquiry-state.ts`; any `EXPIRED` enquiry or still-open enquiry with `expiresAt <= now` now leaves active queues such as Awaiting Guest Payment and Payment Review. Updated `src/pages/HostEnquiries.tsx` with a separate `Passed` section, defensive inactive copy, and no approve/payment-confirm controls for passed cards. Existing backend expiry already marks stale rows `EXPIRED` on reads and syncs listing availability for approved enquiries, which releases `APPROVED_HOLD` blocks; this frontend change fail-closes the UI while waiting for that refresh. Updated tests in `tests/inquiry-state.test.ts`, `tests/ui/host-enquiries.test.tsx`, and stale active fixture dates in `tests/ui/host-dashboard.test.tsx`. Verification passed: `npx tsx --test tests/inquiry-state.test.ts`, `npm run test:ui -- tests/ui/host-enquiries.test.tsx tests/ui/host-dashboard.test.tsx`, `npm run lint`, and `npm run build`. Author signature: (|/) Klaasvaakie

- 2026-06-07: Added a host onboarding tutorial at `/host/onboarding` to make the host workspace easier for new users. The tutorial walks hosts through account/KYC setup, required banking/payment instructions in `/account`, first listing creation, availability setup, quick replies in `/host/quick-replies`, and enquiry handling. Added the route in `src/components/AppRoutes.tsx`, sidebar navigation in `src/components/HostLayout.tsx`, and dashboard entry points in `src/pages/HostDashboard.tsx`. Added focused UI coverage in `tests/ui/host-onboarding-tutorial.test.tsx` and `tests/ui/host-dashboard.test.tsx`. Verification passed: `npm run test:ui -- tests/ui/host-onboarding-tutorial.test.tsx tests/ui/host-dashboard.test.tsx`, `npm run lint`, and `npm run build`. Browser route probe reached `http://127.0.0.1:3000/host/onboarding`, but visual inspection was limited by the protected host-session requirement and the dev server being killed during foreground timeout. Author signature: (|/) Klaasvaakie

- 2026-06-07: Fixed the real marketplace/dashboard availability mismatch for listing `durban beach` (`4a4201e6-d8b9-44d0-832f-a483a8865af2`). Production showed the host dashboard had 17 booked nights, while public `/listings` returned only stale May manual `blockedDates` and no `availabilityBlocks`. Root cause: catalog hydration treated booking DB timestamps as strings and called `.slice(...)`; Encore returned `Date` objects, so hydration threw, swallowed the error, and fell back to stale legacy listing data. Also normalized booking timestamps through the South African listing calendar day instead of UTC slicing so `2026-05-31T22:00:00.000Z` becomes the occupied night `2026-06-01`. Updated `encore/catalog/availability.ts` and `encore/catalog/api.ts`, including the compatibility mapper so `blockedDates` is rebuilt from hydrated availability blocks for marketplace surfaces that still consume the legacy field. Added regression coverage in `tests/catalog-availability.test.ts` and `tests/catalog-marketplace-availability-source.test.ts`. Verification passed: focused availability tests, `npx tsc --noEmit -p encore/tsconfig.json`, `npm run lint`, `npm test`, and `npm run build`. Shipped commits `dcf2589` and `ec6d78d`, Encore prod deploys `1ve58huagipe20bg2n3g` and `1ve59j6agipe20bg2n50`; live `/api/encore/listings?status=active` probe confirmed `durban beach` now returns booked nights in `blockedDates`, including `2026-06-01` through `2026-06-09` and `2026-06-19` through `2026-06-22`. Author signature: (|/) Klaasvaakie

- 2026-06-07: Cleared the Dependabot/npm audit dependency vulnerabilities without broad framework churn. Ran `npm update postcss qs body-parser express react-router react-router-dom`, which resolved `react-router-dom` / `react-router` to `7.17.0`, `express` to `4.22.2`, `body-parser` to `1.20.5`, `qs` to `6.15.2`, and `postcss` to `8.5.15` in `package-lock.json`. `npm audit --json` now reports zero vulnerabilities. Verification passed: `npm run lint`, `npm test`, and `npm run build`. Author signature: (|/) Klaasvaakie

- 2026-06-07: Fixed marketplace listing availability drift where booked dates could still show available on public search/listing detail when a listing already had manual availability blocks. Root cause: `ensureListingAvailabilityHydrated` returned immediately when any ledger block existed, so public `/listings` could skip refreshing `BOOKED` / `APPROVED_HOLD` blocks from active bookings while the host availability calendar looked correct through its own sync path. Updated `encore/catalog/api.ts` so marketplace hydration always reads active booking rows and refreshes booking-owned availability blocks even when manual blocks already exist. Added `tests/catalog-marketplace-availability-source.test.ts` to guard against the early-return regression. Verification passed: `npx tsx --test tests/catalog-marketplace-availability-source.test.ts tests/listing-availability-ui.test.ts tests/catalog-availability.test.ts`, `npx tsc --noEmit -p encore/tsconfig.json`, `npm run lint`, `npm test`, and `npm run build`. Committed fix `1ff7a78`, pushed to GitHub `main`, pushed to Encore staging deploy `1ve4uvmagipe20bg2mtg` and prod deploy `1ve4uvuagipe20bg2mu0`, and live-probed `https://www.idealstay.co.za/api/encore/listings?status=active` returning `200`. Author signature: (|/) Klaasvaakie

- 2026-06-07: Repaired a real failed premium activation after Yoco checkout succeeded but no webhook/status reconciliation activated the account. Staging payment intent `d57d2a9f-55b5-4688-971d-727a56d1cd93` for `wimpievanloggenberg@gmail.com` was still `pending` with host plan `premium`; identity still showed `standard`. Manually applied the fulfillment effects: marked the payment intent `paid`, created active premium monthly subscription `974728eb-406c-4b72-9e2b-fa410f42cdbd` through 2026-07-07, updated identity `host_plan` to `premium`, and converted `host_billing_accounts` to `paid/premium/active/card_on_file=true`. Added a backend-first Yoco return path so future success URLs hit `/api/encore/billing/payments/:paymentId/return?billingStatus=success`, reconcile the payment on the backend in test mode, then redirect to `/pricing` with `payment_id`. Added `encore/billing/payment-return.ts` and payment contract coverage. Verified with DB readback, `npx tsx --test tests/payment-yoco-contracts.test.ts`, `npx tsc --noEmit -p encore/tsconfig.json`, `npm run lint`, `npm test`, and `npm run build`. Committed hotfix `1b3d977`, pushed to Encore staging deploy `1ve4mcuagipe20bg2mqg` and prod deploy `1ve4mcuagipe20bg2mr0`, pushed to GitHub `main`, and live-probed `https://www.idealstay.co.za/api/encore/billing/payments/probe-payment-id/return?billingStatus=success` returning `303` to `/pricing?billing_status=success&payment_id=probe-payment-id`. Author signature: (|/) Klaasvaakie

- 2026-06-07: Reviewed the current IdealTrue dependency stack for libraries that would reduce brittleness before launch. Existing stack already covers UI primitives, routing, tests, Encore API access, Playwright, Vitest, and MSW. Strongest recommendations are TanStack Query for server-state ownership, Zod for runtime contract/config/form validation, React Hook Form for large mutable forms, and Sentry or equivalent error monitoring before public launch. Avoid adding Redux/state-machine/form-builder complexity unless a specific repeated failure justifies it. Author signature: (|/) Klaasvaakie

- 2026-06-07: Added and integrated the recommended launch-hardening libraries. Installed `@tanstack/react-query`, `zod`, `react-hook-form`, `@hookform/resolvers`, and `@sentry/react`. Added a Query Client wrapper and wired `QueryClientProvider` into `src/main.tsx`; migrated `src/hooks/use-platform-data.ts` from manual effect/reload/polling state to TanStack Query cache keys, invalidation, background booking refresh, and cache updates. Added Zod-backed frontend runtime config validation plus optional Sentry initialization with `.env.example` keys. Refactored `src/pages/SignupPage.tsx` field state onto React Hook Form without changing the existing signup behavior. Added `tests/client-runtime-libraries.test.ts` for Query Client defaults, runtime config validation, and Sentry gating. Verification passed: `npx tsx --test tests/client-runtime-libraries.test.ts`, `npm run test:ui -- tests/ui/signup-page.test.tsx tests/ui/pricing-page.test.tsx`, `npm run lint`, `npm test`, and `npm run build`. NPM still reports 6 audit findings after install; do not run blind `npm audit fix` in the release path without reviewing breakage. Author signature: (|/) Klaasvaakie

- 2026-06-07: Stabilized the Yoco payment client and pricing checkout flow using the newly added libraries. Added Zod validation in `src/lib/billing-client.ts` for billing payment creation responses, payment status responses, checkout status responses, and return URL params so malformed provider/backend responses cannot redirect users or drive checkout reconciliation. Added `parseBillingReturnParams` so only `success`, `cancelled`, or `failed` with a `payment_id` or legacy `checkout_id` can trigger payment resolution. Updated `src/pages/PricingPage.tsx` to use TanStack Query for host billing-account loading and TanStack mutations for subscription/managed-hosting checkout starts, while preserving the existing polling reconciliation for Yoco return/webhook lag. Added payment contract tests for malformed checkout responses and return-param parsing, and updated pricing UI tests to run under a real Query Client provider. Verification passed: `npx tsx --test tests/payment-yoco-contracts.test.ts`, `npm run test:ui -- tests/ui/pricing-page.test.tsx`, `npm run lint`, `npm test`, and `npm run build`. Remaining real-world risk is provider-side Yoco 3DS/webhook delivery; this patch hardens app-side contracts but does not remove the need for live/test checkout probes against the deployed staging backend. Author signature: (|/) Klaasvaakie

- 2026-06-06: Fixed host listing discounts so they affect the real marketplace and booking price instead of only showing as a creation-time/card badge. Added `src/lib/listing-pricing.ts` for frontend discounted nightly-rate display, wired Explore price filtering, listing cards, map info windows, and listing detail booking totals to use the discounted rate, and updated backend booking total calculation in `encore/booking/workflow.ts` / `encore/booking/api.ts` to enforce `discountPercent` server-side. Added regression coverage in `tests/listing-pricing.test.ts`, `tests/ui/listing-pricing.test.tsx`, and `tests/booking-workflow.test.ts`. Verified with `npx tsx --test tests/listing-pricing.test.ts tests/booking-workflow.test.ts`, `npx vitest run tests/ui/listing-pricing.test.tsx`, and `npm run lint`. Author signature: (|/) Klaasvaakie

- 2026-06-06: Updated `docs/app-flow-diagram.mmd` and regenerated `docs/app-flow-diagram.svg` so pricing/subscription billing now shows the current Checkout-backed `POST /billing/payments` flow, returns through `payment_id`, and polls `GET /billing/payments/:paymentId`. Removed the stale `Booking Chat / Previous Chat Implementation` label in favor of booking-scoped messaging wording. Strengthened `tests/workflow-coverage.test.ts` so the workflow docs, app-flow Mermaid source, and generated SVG reject obsolete billing checkout labels and stale messaging labels. Verified with `npx tsx --test tests/workflow-coverage.test.ts`. Author signature: (|/) Klaasvaakie

- 2026-06-05: Rewrote the root `README.md` around proprietary ownership, strict no-license access, production/runtime gates, and the current Encore-first architecture. Added a root `LICENSE` proprietary notice, replaced the stale Encore template README, and changed root/backend package metadata plus lockfiles to `UNLICENSED` with Klaasvaakie as author. Verified the manifests report `UNLICENSED`; remaining `MPL-2.0` mentions are third-party dependency licenses, not the app license. Author signature: (|/) Klaasvaakie

- Deployed the frontend to Vercel production and aliased it to `https://www.idealstay.co.za`. Verified the live host with `scripts/live-smoke.mjs` against the production URL; public health and listings probes passed. No Encore Cloud backend deployment was possible from this worktree because the repo has no linked Encore Cloud app metadata.

- Moved the managed hosting package onto the same Yoco checkout flow as the other paid offerings by adding a real `managed_hosting` billing purpose, wiring `src/pages/PricingPage.tsx` to a new managed-hosting checkout helper, and updating the related client/admin type unions and tests. Verified with `npx vitest run tests/ui/pricing-page.test.tsx tests/payment-yoco-contracts.test.ts tests/workflow-client-contracts.test.ts tests/ui/signup-page.test.tsx` plus `npm run lint`.

- Updated the hosted/managed package price in `src/pages/PricingPage.tsx` from a vague custom-package label to `R650 / month`, and aligned the managed-host signup note in `src/pages/SignupPage.tsx` plus the corresponding UI test assertion in `tests/ui/signup-page.test.tsx`. Verified with `npx vitest run tests/ui/signup-page.test.tsx tests/ui/pricing-page.test.tsx` and `npm run lint`.

- Proved the gateway itself in test mode by creating a real Yoco Checkout API session with the documented test secret key; the response came back `200` with `processingMode: "test"` and a checkout redirect URL. This confirms the backend can create checkout sessions in test mode, which is the path the subscription flow now uses.

- Removed the remaining backend Yoco payment-link internals, leaving only checkout-based billing flows in runtime code. `encore/billing/api.ts`, `encore/billing/yoco.ts`, `src/lib/billing-client.ts`, and the payment-link contract tests were pruned down to checkout-only paths; `README.md` now documents Yoco test mode with `YOCO_PAYMENT_MODE=test`, `YOCO_TEST_SECRET_KEY`, and the current Yoco test card. Verified with `npm run lint` and `npx vitest run tests/payment-yoco-contracts.test.ts tests/ui/pricing-page.test.tsx tests/workflow-client-contracts.test.ts`.

- Removed the remaining subscription-specific legacy Yoco payment-link route from `encore/billing/api.ts`, leaving subscription packages on the real checkout path only. Confirmed there are no remaining subscription payment-link references in `src`, `encore`, or `tests`, and re-verified with `npx vitest run tests/ui/pricing-page.test.tsx tests/payment-yoco-contracts.test.ts tests/workflow-client-contracts.test.ts` plus `npm run lint`.

- Switched the pricing page subscription purchase flow from the generic payment-intent/payment-link path to the real Yoco checkout endpoint by wiring `src/pages/PricingPage.tsx` to `createSubscriptionCheckout`, removing the frontend `createSubscriptionPaymentLink` export from `src/lib/billing-client.ts`, and deleting the subscription payment-link contract tests that no longer represent the shipped flow. Verified with `npx vitest run tests/ui/pricing-page.test.tsx tests/ui/social-dashboard.test.tsx` and `npm run lint`.

- Realigned the booking workflow truth surfaces by updating `tests/e2e/booking-payment-review.spec.ts` to the shipped guest CTA label, fixing `tests/fixtures/workflows.ts` and `tests/workflow-coverage.test.ts` to use the real `APPROVED/INITIATED/paymentSubmittedAt` and `BOOKED/COMPLETED` lifecycle states plus structured decline reasons, and correcting `docs/booking-and-enquiry-workflow.md` so it no longer claims decline reasons are missing.
- Removed overlapping booking workflow ownership by introducing shared host booking actions in `src/hooks/use-host-booking-actions.ts`, making `src/pages/HostDashboard.tsx` derive directly from canonical booking props instead of a local shadow copy, reusing the same host action path in `src/pages/HostEnquiries.tsx`, and tightening `src/pages/SignupPage.tsx` plus `src/pages/SocialDashboard.tsx` so auth mode and studio route state stop fighting duplicated local state.
- Shrunk the guest `My Stays` cards in `src/pages/GuestDashboard.tsx` by tightening image height, padding, typography, summary spacing, and action button sizes, while also allowing a denser desktop grid so the page shows meaningfully smaller stay cards without removing booking/payment detail.
- Fixed dashboard mobile responsiveness by giving `src/components/HostLayout.tsx` a real mobile slide-out nav instead of a dead menu button, adding the same mobile-nav treatment to `src/pages/AdminDashboard.tsx`, tightening narrow-screen action/header layouts in `src/pages/HostDashboard.tsx` and `src/pages/GuestDashboard.tsx`, and locking the shell behavior with `tests/ui/dashboard-mobile-shell.test.tsx`.
- Reworked `src/pages/PricingPage.tsx` so the public pricing story now pressures on lost booking margin, ownership, direct control, and visibility instead of discounts or free-month offers; removed the public annual-discount/founding-voucher sections, kept the plan CTAs on simple monthly pricing, and aligned the visible plan entitlements with the current product reality.
- Fixed signup truthfulness so `encore/identity/api.ts` no longer throws a full registration failure after the user row is already created just because the verification email dispatch fails; signup now returns a live session with `verificationEmailStatus`, `src/contexts/AuthContext.tsx` / `src/pages/SignupPage.tsx` surface a warning instead of a false success, and `tests/signup-flow.test.ts` plus `tests/workflow-client-contracts.test.ts` lock the contract.
- Fixed the auth entry flow so `/signup?mode=signin`, header/mobile Sign In actions, verify-email success, and reset-password success all land on the real sign-in panel while preserving referral params during signup/signin toggles; covered it in `tests/e2e/auth-account.spec.ts`.
- Fixed auth email dispatch in `encore/identity/email.ts` so missing optional Encore secrets no longer abort verification/reset email sends before Resend is called, while missing required delivery/app URL config now fails with explicit errors.
- Moved the Content Studio side-rail controls out of `src/pages/SocialDashboard.tsx` into a compact Studio Tools dropdown, expanded the Social Media group in `src/components/HostLayout.tsx` with the Content Studio tool entries, and added UI coverage in `tests/ui/social-dashboard.test.tsx` so the dropdown keeps Create Post, tools, and wallet access.
- Made messaging process-aware by adding role-specific booking-stage context and quick actions in `src/lib/inquiry-state.ts`, wiring `src/components/ChatModal.tsx` and `src/pages/HostInbox.tsx` to show the current host/guest workflow step, and aligning message suggestion typing across `src/types.ts`, `encore/shared/domain.ts`, and `encore/messaging/api.ts`.
- Added `tests/inquiry-state.test.ts` coverage for host decision, guest payment, and confirmed-stay messaging action contracts so chat suggestions stay tied to real booking state instead of static role-only buttons.
- Established `docs/workflow-validation-matrix.md` as the source-of-truth workflow checklist, tying routes, Encore services/endpoints, current tests, gaps, fixture requirements, priority expansion, async/provider workflows, and CI gates to each product workflow; linked it from `README.md`.
- Added executable workflow coverage infrastructure in `tests/fixtures/workflows.ts`, `tests/workflow-coverage.test.ts`, and `tests/workflow-client-contracts.test.ts` so every documented workflow has deterministic fixtures, machine-checked inventory coverage, and broader client contract tests for auth, KYC, content, messaging, reviews, referrals, and checkout status.
- Added `tests/e2e/auth-account.spec.ts` to cover the first missing high-priority Playwright workflow: signup with referral code, email verification, signin, password reset request, and reset-token completion against deterministic Encore route mocks.
- Fixed `src/pages/SignupPage.tsx` so reset-password mode no longer inherits signup-only role/name submit requirements, allowing valid reset-token submissions to proceed.
- Tightened `src/components/ListingDetail.tsx` date selection so same-day calendar picks no longer close as zero-night stays, and booking submission now rejects checkout dates that are not after check-in.
- Wired account shutdown paths in `encore/identity/api.ts` and `encore/identity/account-status.ts` so suspending, deactivating, or attempting to delete a user now pauses any active host listings before the account lifecycle action continues, preventing live inventory from surviving restricted accounts.
- Turned billing greylisting into a real host operations lock by adding a reusable restriction guard in `encore/billing/host-billing*.ts`, enforcing it across host listing mutations in `encore/catalog/api.ts` and host booking actions in `encore/booking/api.ts`, and surfacing the backend error in `src/pages/HostListings.tsx` so greylisted hosts cannot quietly keep reactivating listings or advancing enquiries.
- Hardened `encore/catalog/api.ts` so public listing reads fail open when host-billing visibility checks or availability-hydration reads cannot hit their newer tables; the marketplace now keeps serving listings instead of 500ing when billing/catalog migrations lag behind.
- Refined `src/features/admin/use-admin-dashboard-data.ts` so admin partial-load toasts now name the failing sections and only use destructive severity when essential admin slices fail, instead of the vague generic warning.
- Fixed Encore cloud deploy blockers in `encore/billing/host-billing-service.ts` and `encore/booking/api.ts` by replacing unsupported cron `endpoint` arrow functions with named async handlers, so backend deploys can compile and ship the newer billing/admin routes.
- Hardened `src/lib/admin-client.ts` and `src/features/admin/use-admin-dashboard-data.ts` so the admin dashboard still renders when the deployed Encore backend is missing the newer host-billing admin endpoints; the billing panel now degrades safely instead of blanking the whole admin screen after the `idealstay.co.za` cutover.

- Performed a repo-wide drift scan on 2026-06-05. Verified `npm run lint` passes. Found one concrete stale-doc mismatch: `docs/app-flow-diagram.mmd` still references `POST /api/encore/billing/subscriptions/checkout` even though the active billing client and backend use the consolidated `POST /billing/payments` path; the generated SVG mirrors the same stale label. Also noted an older wording artifact in the flow diagram header: `Booking Chat / Previous Chat Implementation`. (|/) Klaasvaakie
- 2026-06-05: Added the missing billing proof seam for the current Yoco Checkout-backed architecture. New `tests/e2e/host-billing.spec.ts` proves `/pricing` starts subscription and managed-hosting payments through `POST /billing/payments`, follows the checkout redirect URL, and resolves the return via `payment_id` plus `billing_status`. Extended `tests/payment-yoco-contracts.test.ts` with accepted Yoco event classification coverage and exact raw-body signature verification, backed by the new pure helper `encore/billing/yoco-signature.ts` used by `encore/billing/yoco.ts`. Verified with `npx tsx --test tests/payment-yoco-contracts.test.ts`, `npx playwright test tests/e2e/host-billing.spec.ts`, and `npm run lint`. Author signature: (|/) Klaasvaakie
- 2026-06-05: Started the full surface coverage/documentation hardening pass. Updated `docs/workflow-validation-matrix.md` and `tests/fixtures/workflows.ts` so billing references the current standard `createBillingPayment` / `getBillingPaymentStatus` path instead of stale checkout wrappers, moved `tests/e2e/host-billing.spec.ts` and `tests/payment-yoco-contracts.test.ts` into current coverage, added `docs/developer-handoff.md` as the new-dev entry point, linked it from `README.md`, and added a `tests/workflow-coverage.test.ts` guard that rejects obsolete billing checkout endpoint names in the matrix. Verified with `npx tsx --test tests/workflow-coverage.test.ts`, `npx tsx --test tests/payment-yoco-contracts.test.ts`, `npx playwright test tests/e2e/host-billing.spec.ts`, and `npm run lint`. Author signature: (|/) Klaasvaakie
- Added server-owned enquiry expiry windows in `encore/booking/workflow.ts` and `encore/booking/api.ts`, enforcing 48-hour unresolved-enquiry expiry, 24-hour approval expiry, automatic `EXPIRED` transitions on reads/mutations, and an hourly expiry cron so stale approvals release their `APPROVED_HOLD` inventory.
- Updated `src/lib/inquiry-state.ts`, `src/pages/HostEnquiries.tsx`, `src/pages/HostDashboard.tsx`, and `src/pages/GuestDashboard.tsx` so host and guest flows show real response/payment/confirmation deadlines and expired-state copy from the canonical booking state instead of vague UI-only messaging.
- Added coverage in `tests/booking-workflow.test.ts` and `tests/inquiry-state.test.ts`, and wrote the implementation notes to `docs/superpowers/plans/2026-04-20-enquiry-expiry.md` plus `docs/booking-and-enquiry-workflow.md`.
- Fixed `src/pages/HostDashboard.tsx` so the enquiry summary card now uses the real host `needs_response` queue instead of a sliced local preview, keeping the dashboard aligned with the enquiry workflow buckets.
- Added `tests/ui/host-dashboard.test.tsx` to lock the host dashboard metric to the full needs-response count and prevent approved enquiries from inflating that card.
- Reduced the verified-state `Continue` button in `src/components/KYCModal.tsx` so the success modal uses a much smaller CTA footprint.
- Updated `src/components/AppNavigation.tsx` so the public header now shows the `Ideal Stay` name next to the logo, making the brand lockup explicit in the main nav.
- Added `public/ideal-stay-logo.png` from the supplied asset and created `src/components/BrandLogo.tsx` to centralize branding.
- Replaced old text/icon branding in `src/components/AppNavigation.tsx`, `src/components/HostLayout.tsx`, `src/pages/AdminDashboard.tsx`, and `src/pages/SignupPage.tsx`.
- Updated `src/App.tsx` loading state and `index.html` favicon links so the logo shows consistently across the site and browser chrome.
- Removed the redundant amber breakage-deposit callout from `src/components/ListingDetail.tsx` and kept the single summary-row version in the booking panel.
- Removed pre-enquiry breakage-deposit visibility from `src/components/ListingDetail.tsx` and `src/components/PropertyCard.tsx` so the deposit is not broadcast on public listing surfaces.
- Restored breakage-deposit visibility inside the date-selected booking summary in `src/components/ListingDetail.tsx`, showing it only after dates are chosen and including it in the displayed pre-enquiry total.
- Added a durable listing-availability ledger in `encore/catalog` so manual host blocks, approved enquiry holds, and booked stays are tracked separately instead of being flattened into one fragile `blocked_dates` array.
- Tightened `encore/booking/api.ts` so booking creation and enquiry approval now fail closed on overlapping availability instead of relying on client-side honesty.
- Updated `src/types.ts`, `src/lib/domain-mappers.ts`, and `src/pages/HostAvailability.tsx` so the host calendar distinguishes manual blocks from approved-payment holds and confirmed stays.
- Added a shared frontend availability helper in `src/lib/listing-availability.ts` and switched `src/pages/ExploreView.tsx` plus `src/components/ListingDetail.tsx` to use end-exclusive stay overlap checks so booked nights are filtered out consistently without mis-handling checkout days.
- Rebuilt `src/pages/HostEnquiries.tsx` into a stronger host operations screen with workflow buckets, summary metrics, aging cues, clearer payment-tracking context, and proper host feedback on approve/decline/confirm actions.
- Added host inquiry workflow helpers in `src/lib/inquiry-state.ts` plus tests in `tests/inquiry-state.test.ts` so enquiry categorization and sorting stay consistent instead of living as page-local guesswork.
- Added durable feature documentation in `docs/booking-and-enquiry-workflow.md` and updated `README.md` so the availability ledger and host enquiry workflow are documented outside the session log.
- Updated `src/App.tsx` and `src/pages/HostDashboard.tsx` so enquiry workflow changes bring the relevant screen to the front and keep active booking UI state in sync instead of leaving users on stale surfaces.
- Extended `src/pages/HostAvailability.tsx` into a real operations tool with availability metrics, bulk range actions, day inspection, and upcoming-constraint tracking, backed by tested helpers in `src/lib/host-availability.ts`.
- Extended the backend availability model in `encore/catalog/api.ts`, `encore/catalog/availability.ts`, `encore/shared/domain.ts`, and migration `encore/catalog/migrations/6_listing_availability_block_notes.up.sql` so manual blocks can be stored as interval records with notes and queried through a server-side availability summary endpoint.
- Updated `src/lib/platform-client.ts`, `src/lib/domain-mappers.ts`, `src/types.ts`, and `src/pages/HostAvailability.tsx` so the host calendar now uses the backend interval/block APIs and server summary instead of local-only blocked-date writes.
- Added coverage in `tests/host-availability.test.ts` for interval compression and note preservation, and kept the availability checks green with focused unit runs.
- Updated `docs/booking-and-enquiry-workflow.md` and `README.md` to document the backend availability-block endpoints, interval semantics, and manual block notes.
- Added a compatibility fallback in `src/lib/platform-client.ts` and `src/pages/HostAvailability.tsx` so the host calendar degrades to the legacy blocked-date endpoint when the deployed Encore backend does not yet expose the new availability summary/block endpoints.
- Fixed `src/pages/HostDashboard.tsx` to treat all open host enquiries as enquiries instead of only pending-decision ones, and removed noisy booking debug logs from `src/hooks/use-platform-data.ts`.
- Raised the Standard host listing-photo cap to 10 in `src/pages/CreateListing.tsx` and `encore/catalog/api.ts`, while keeping Professional/Premium at 20 and leaving the single showcase-video slot unchanged.
- Added a UI regression test in `tests/ui/create-listing.test.tsx` to lock the Standard-plan photo cap at 10 and verify the media step still exposes a single video uploader.
- Removed Standard-tier showcase video access across `src/pages/CreateListing.tsx`, `encore/catalog/api.ts`, `encore/catalog/host-plan.ts`, `src/pages/PricingPage.tsx`, and `encore/shared/domain.ts` so Standard is now 10 photos and 0 video while paid tiers keep 20 photos and 1 video.
- Added host billing lifecycle infrastructure in `encore/billing/host-billing.ts`, `encore/billing/host-billing-service.ts`, and migration `encore/billing/migrations/7_host_vouchers_and_billing.up.sql` to support voucher-backed onboarding, billing-cycle state, reminder windows, card-on-file tracking, greylisting, and billing event history.
- Seeded the first 100 founding-host voucher PINs in the new billing migration and exported the same codes to `docs/host-voucher-pins-2026-04-20.csv` for distribution.
- Exposed new billing APIs in `encore/billing/api.ts` plus frontend clients in `src/lib/billing-client.ts` and `src/lib/admin-client.ts` for voucher redemption, host billing account reads, card capture, admin host billing reads, and greylist control.
- Updated `encore/catalog/api.ts` so greylisted hosts are hidden from public listing surfaces and direct public listing fetches, while greylisting also pauses active listings by forcing them inactive in the billing service.
- Reworked `src/pages/HostDashboard.tsx` into a real host billing surface that shows billing source, cycle dates, reminder state, greylist state, and a billing-card capture form instead of a fake static plan card.
- Extended admin billing visibility through `src/features/admin/use-admin-dashboard-data.ts`, `src/features/admin/dashboard-sections.tsx`, and `src/pages/AdminDashboard.tsx` so admins can see billing-greylisted hosts and toggle greylist state directly from the user management surface.
- Added coverage in `tests/host-billing-lifecycle.test.ts`, `tests/catalog-billing-helpers.test.ts`, `tests/api-clients.test.ts`, and updated UI/MSW tests so the new entitlements, voucher lifecycle, and billing endpoints are locked down by focused checks.
- Tightened voucher-host billing UX in `src/lib/host-billing-ui.ts`, `src/pages/HostDashboard.tsx`, and `tests/host-billing-ui.test.ts` so the dashboard now shows explicit countdown, urgency, reminder history, and required-action copy ahead of greylisting instead of generic billing text.
- Tightened the host enquiry snapshot on `src/pages/HostDashboard.tsx` so approved enquiries waiting on guest payment or payment confirmation now get dedicated summary cards plus an expiring-hold watchlist, backed by shared workflow helpers in `src/lib/inquiry-state.ts`, reused in `src/pages/HostEnquiries.tsx`, and locked down in `tests/inquiry-state.test.ts` plus `tests/ui/host-dashboard.test.tsx`.
- Tightened guest `My Stays` cards in `src/pages/GuestDashboard.tsx` to show stay value, breakage deposit, full guest exposure, and explicit payment-state copy from `src/lib/inquiry-state.ts`, with regression coverage in `tests/inquiry-state.test.ts` and new UI coverage in `tests/ui/guest-dashboard.test.tsx`.
- Centralized guest and host enquiry deadline copy in `src/lib/inquiry-state.ts`, rewired `src/pages/GuestDashboard.tsx` and `src/pages/HostEnquiries.tsx` to use those shared helpers, and expanded `tests/inquiry-state.test.ts` plus `tests/ui/guest-dashboard.test.tsx` so guest stay cards stay explicit about deposit, full exposure, payment state, and hold-release timing.
- Added structured enquiry decline reasons across `encore/booking/api.ts`, `encore/ops/*`, `src/lib/inquiry-state.ts`, `src/pages/HostEnquiries.tsx`, `src/pages/HostDashboard.tsx`, `src/features/admin/dashboard-sections.tsx`, and the new `src/components/InquiryDeclineDialog.tsx`, so hosts must choose a persisted decline reason, guests/admins see that reason on the booking, and notifications stop using the generic decline message.
- Compressed the host metric strip in `src/pages/HostDashboard.tsx` into compact cards with just the count, optional percentage chips, and attention dots for live host queues; updated `tests/ui/host-dashboard.test.tsx` to lock that leaner contract instead of the removed helper copy.
- Added `tests/e2e/kyc-admin-review.spec.ts` to cover the KYC workflow from host submission through admin secure-preview review and approval, then updated the executable workflow registry and `docs/workflow-validation-matrix.md` to move KYC Playwright coverage out of the gap list.
- Hardened Content Studio coverage by extracting backend content entitlement debit decisions into `encore/billing/content-entitlements.ts`, adding focused unit coverage, tightening the content client contract payload assertions, adding `tests/ui/social-dashboard.test.tsx` for generate/edit/schedule behavior, and fixing `src/pages/SocialDashboard.tsx` so workspace reloads no longer erase a newly generated draft after selection.
- Restyled `src/pages/SocialDashboard.tsx` into a Plann-inspired Content Studio workspace with a tool rail, platform chips, idea-source modes, output-type selectors, contextual credit controls, listing/media selection, and a clearer generator call-to-action while preserving the existing draft/visual generation contract.
- Made the host Social Media nav real by linking New Post Ideas, Quick Templates, Media Collections, and Content Calendar to query-driven Content Studio sub-screens in `src/components/HostLayout.tsx` and `src/pages/SocialDashboard.tsx`.
- Wired Quick Templates, Media Collections, and Content Calendar inside `src/pages/SocialDashboard.tsx`, added accessible token top-up checkout controls, and expanded `tests/ui/social-dashboard.test.tsx` to verify template selection, media-source selection, and content-token top-up redirects.
- Replaced the old beach-pin brand asset with the new Ideal Stay lockup in `public/ideal-stay-logo.svg` and regenerated `public/ideal-stay-logo.png`; updated `src/components/BrandLogo.tsx` so all shared logo placements use the new artwork.
- Reworked browser chrome assets so `public/favicon.svg` and `public/apple-touch-icon.png` use only the mark portion of the new logo, then updated `index.html` to reference those favicon-specific files.
- Simplified the public header in `src/components/AppNavigation.tsx` to avoid duplicating the brand name beside the new full lockup.
- Switched `src/components/BrandLogo.tsx` back to the PNG lockup so website logo placements use `public/ideal-stay-logo.png` while favicon assets stay mark-only.
- Replaced `public/ideal-stay-logo.png` and `public/apple-touch-icon.png` with Klaas's supplied `Ideal Stay Logo (1).png`, and pointed `index.html` favicon/touch-icon links at the same canonical PNG so in-app logo placements and browser chrome use the exact same brand asset.
- 2026-06-03: Inspected the Yoco billing seam in `encore/billing/yoco.ts`, `encore/billing/api.ts`, and `encore/billing/webhook-classification.ts`. The integration is already using the documented server-side checkout flow and webhook signature pattern, so the remaining likely failure points are config and account-side setup: missing/incorrect `YOCO_SECRET_KEY`, missing/incorrect `YOCO_WEBHOOK_SECRET`, webhook registered against the wrong public backend URL, or a live/test mode mismatch between the Yoco dashboard and the backend secrets. Author signature: (|/) Klaasvaakie
- 2026-06-03: Hardened Yoco checkout creation by adding idempotency keys from the internal checkout id, preserving the provider mode field, and surfacing invalid checkout responses more clearly. This reduces duplicate checkout churn and makes provider-side failures less opaque. Author signature: (|/) Klaasvaakie
- 2026-06-03: Stored the newly created Yoco live secret in `.env.local` so local billing requests can hit the live checkout API. The remaining blocker for full payment confirmation is the matching webhook secret, which still has to come from the Yoco dashboard. Author signature: (|/) Klaasvaakie
- 2026-06-03: Probed Yoco's webhook list API with the newly created live key and received `403 Forbidden` on `/api/webhooks/subscriptions`, so this key does not have webhook-management permission or the webhook setup must be done from the dashboard. This means checkout API access and webhook administration are not the same capability here. Author signature: (|/) Klaasvaakie
- 2026-06-03: Created the Yoco webhook subscription via `POST https://api.yoco.com/v1/webhooks/subscriptions/` using the live key. Yoco accepted the webhook for the Ideal Stay callback URL and returned a one-time webhook secret that was saved locally. Author signature: (|/) Klaasvaakie
- 2026-06-03: Replaced the brittle Yoco hosted subscription checkout path on the public pricing page with the four actual payment links the user created (`pay.yoco.com/r/mdOO6x`, `4nJJ1B`, `2DzzAw`, `2BGGAB`) so users stop getting dumped into the broken hosted checkout complete flow with Google Pay/fingerprint/CSP failures. Author signature: (|/) Klaasvaakie
- 2026-06-03: The user also surfaced the live Yoco key pair from the dashboard. This means the live checkout/webhook path can be configured directly from the dashboard-backed live account, but it still does not prove the hosted checkout page itself is healthy. Author signature: (|/) Klaasvaakie
- 2026-06-03: Added a pricing-page regression test so signed-in hosts now open the correct Yoco payment link for the selected plan, while managed hosting still routes through signup. This locks the product purchase surface to the payment-link flow instead of the broken hosted checkout API. Author signature: (|/) Klaasvaakie
- 2026-06-03: Re-analyzed the payment work after repeated Yoco hosted checkout failures. Current state is mixed: public pricing uses static Yoco payment links, while host billing setup and content credit top-ups still use server-created Yoco checkouts and checkout-status polling. Production direction should consolidate on a payment-link-first architecture with server-owned purchase sessions, Yoco order/payment verification, webhook reconciliation, separate test/live env config, and admin visibility/manual resolution. Author signature: (|/) Klaasvaakie
- 2026-06-03: Implemented the production payment-link-first Yoco seam. Added server-owned `billing_payment_link_sessions`, Yoco API payment-link creation via `POST /v1/payment_links/`, documented Orders API reconciliation via stored `order_id`, webhook matching for checkout and payment-link sessions, and frontend flows for plan upgrades, host billing setup, and content credit top-ups. Kept legacy checkout endpoints/polling for compatibility, but new user-facing paid flows now create traceable server-owned payment links instead of relying on static product links or the brittle hosted checkout path. Verification passed: `npm run lint`, focused client contract tests, focused UI tests, and `npm run build`. Author signature: (|/) Klaasvaakie
- 2026-06-03: Committed the server-owned Yoco payment-link implementation as `29da601`, pushed `main` to GitHub and Encore, and deployed Vercel production. Encore triggered staging deploy `1vbaas6agip0j3m4k1b0` and prod deploy `1vbaas6agip0j3m4k1bg`; Vercel deployment `dpl_9woXHyqwnVgSeZBcXGtekSRfbjzy` reached `READY` and was aliased to `https://www.idealstay.co.za` plus `https://idealstay.co.za`. Live `/pricing` returned HTTP 200 after deploy. Author signature: (|/) Klaasvaakie
- 2026-06-03: Added a safe Yoco test-payment option without changing production's default behavior. Backend Yoco calls now resolve `YOCO_PAYMENT_MODE` as `live` by default and switch to `YOCO_TEST_SECRET_KEY` only when the environment is explicitly set to `test`; payment-link responses and stored sessions expose `providerMode` so test/live orders can be audited. Documented the switch in `.env.example`. Verification passed: `npm run lint`, focused client contract tests, focused UI tests, `npm run build`, and tracked-secret scan. Author signature: (|/) Klaasvaakie
- 2026-06-03: Configured the Encore `YOCO_TEST_SECRET_KEY` secret across environment types without enabling test mode globally. `YOCO_PAYMENT_MODE` remains unset, so runtime defaults to live until an operator deliberately sets the mode to `test` for a controlled Yoco test-card run and then switches it back to `live`. Author signature: (|/) Klaasvaakie
- 2026-06-03: Investigated production `500` on `POST /api/encore/billing/subscriptions/payment-link`. Encore log streaming was unavailable with a platform 500, but the endpoint stopped pre-auth failing after explicitly configuring `YOCO_PAYMENT_MODE=live`; unauthenticated probes now return the expected `401`. Hardened `encore/billing/yoco.ts` so missing optional mode secrets fall back to `live` instead of breaking payment-link creation. Verification passed: `npm run lint`, focused client contract tests, and focused UI payment tests. Author signature: (|/) Klaasvaakie
- 2026-06-03: User called out the payment integration churn and proposed rebuilding the Yoco system from the ground up. Current recommendation: do not delete product billing state, entitlements, vouchers, subscriptions, or fulfilment logic; rebuild the provider adapter and checkout surface into one standard Yoco integration path with one payment intent table, one create-payment endpoint, one status endpoint, one webhook handler, explicit live/test mode, and no mixed legacy checkout/static-link paths. Author signature: (|/) Klaasvaakie
- 2026-06-03: Implemented the standard Yoco rebuild slice. Added canonical `billing_payment_intents`, `POST /billing/payments`, and `GET /billing/payments/:paymentId`; all new pricing, host billing setup, and content credit top-up frontend flows now call one `startBillingPayment(...)` client instead of separate checkout/payment-link wrappers. Webhooks now resolve payment intents first by Yoco `order_id`, with legacy checkout/payment-link tables left as compatibility/audit fallback only. Verification passed: `npm run lint`, focused workflow/API client tests, focused UI payment tests, `npm run build`, and tracked-secret scan. Author signature: (|/) Klaasvaakie
- 2026-06-03: Root-caused the remaining logged-in payment 500. The new intent path incorrectly used Yoco Payment Links API, which requires Yoco API OAuth/JWT scoped credentials; the configured developer-console live key was rejected with `401`, and the same key was rejected by Checkout API with `403`. Yoco docs state Checkout API is the correct API for accepting online payments and uses separate checkout secret keys. Switched canonical payment intents to create Yoco Checkout API sessions, added `provider_checkout_id` to intents, updated webhook/status matching to checkout metadata/id, and replaced Encore `YOCO_SECRET_KEY` with the working Checkout API live secret. Verification passed: direct Checkout API probes with test/live checkout secrets, `npm run lint`, focused client/UI tests, `npm run build`, and tracked-secret scan. Author signature: (|/) Klaasvaakie
- 2026-06-03: Finished the production payment rebuild hardening pass. Legacy `/billing/*/payment-link` endpoints now delegate to the standard Checkout-backed payment-intent path instead of calling Yoco Payment Links, and legacy payment-link status URLs can resolve payment intent ids. Pricing, host billing setup, and content credits now resolve `payment_id` return URLs from the standard flow while retaining `checkout_id` fallback for older sessions. Verification passed: `npm run lint`, focused API/client tests, focused UI tests, Encore typecheck, `npm run build`, `git diff --check`, and tracked-secret scan with only signature-parsing code hits. Author signature: (|/) Klaasvaakie
- Replaced `public/favicon.svg` with Klaas's supplied `Ideal Stay Logo (1).svg` and pointed the browser favicon link in `index.html` at `/favicon.svg` while leaving the site logo and apple touch icon untouched.
- Reverted the prior carousel/auth behavior change at Klaas's request, then replaced the old `from-slate-900 to-blue-600` gradient styling on selected controls, featured carousel buttons, search action, pricing pills, and listing-form controls with the primary logo blue.
- Updated the same de-gradient controls to Klaas's exact logo cyan `#08a8c8`.
- Pulled the remaining search-bar gradient icon buttons into the same solid `#08a8c8` treatment after verifying listings were failing only on the preview server without the API proxy.
- Replaced the remaining listing-creation pricing redirect button gradient with the same solid `#08a8c8` brand cyan.
- Updated signup/header auth actions, signup mode toggles, and selected role accents to the exact `#08a8c8` brand cyan.
- Rebased the app's primary design token in `src/index.css` from the old blue to `#08a8c8` so listing-detail buttons, focus rings, calendar accents, and remaining `primary` controls inherit the logo color.
- Added Klaas's wide inline logo as `public/ideal-stay-logo-inline.png`, extended `BrandLogo` with compact/inline variants, and used the inline lockup in header, signup, loading, host, and admin contexts where horizontal branding fits.
- Ported the Ideal-today footer/documentation surface into IdealTrue by copying the legal documentation pages into `src/pages/legal`, adding `src/components/AppFooter.tsx`, wiring public footer rendering in `src/App.tsx`, and exposing the legal routes in `src/components/AppRoutes.tsx`.
- Fixed the public legal-page shell in `src/App.tsx` so `/host-agreement` is no longer misclassified as a `/host` dashboard route and keeps normal public navigation/footer exits.
- Tightened the footer, header logo sizing, legal-page wrappers, and explore category filter styling so the public footer is shorter, `/host-agreement` no longer carries a duplicate page container, the header logo is more legible without increasing nav height, and the category row reads as light morphic controls instead of a boxed strip.
- Preserved signed-out booking intent across the auth wall by adding URL-based booking return helpers, hydrating listing detail dates/guests from return params, honoring safe auth `returnTo` paths, and covering the unauthenticated Book CTA in `tests/ui/booking-auth-intent.test.tsx`.
- Finished the Content Studio schedule/publish tracking pass by making `src/pages/SocialDashboard.tsx` render draft lifecycle dates from status-owned timestamps, adding `tests/e2e/content-studio.spec.ts` for generate -> schedule -> revisit -> publish logging on `/host/social`, and updating the workflow matrix/fixture coverage registry.
- Added backend-owned host message quick replies in `encore/messaging` with a `/messages/quick-replies/me` settings contract, a new `/host/quick-replies` host screen, and chat-modal wiring so House Rules, Directions, Payment Info, Check-in, and Checkout actions can use each host's real saved copy instead of static placeholders.
- Added an in-chat guest "Submit proof of payment" action for approved unpaid enquiries, wiring it to the existing payment-proof dialog so the message workflow no longer only talks about proof submission while hiding the actual action elsewhere.
- Fixed the booking-to-chat payment handoff by adding `encore/messaging/subscriptions.ts` to project booking workflow changes into system messages, adding a host-side in-chat "Send payment details" CTA in `src/components/ChatModal.tsx`, relabeling the guest action in `src/pages/GuestDashboard.tsx` to "Send Proof of Payment", and making `src/pages/HostInbox.tsx` label payment-stage chat actions by the real next step instead of a generic open-chat button.
- Fixed host enquiry approval compatibility on lagging backends by routing booking availability sync through `syncBookingAvailabilityWithCompatibility(...)` in `encore/catalog/api.ts` and `encore/booking/api.ts`; when the newer `listing_availability_blocks` schema is unavailable, approval now falls back to legacy `blocked_dates` sync instead of throwing a generic internal error.
- 2026-05-23: Scoped the managed-hosting package plan. Key seams: pricing tiers are hard-coded to `standard|professional|premium`; booking payment instructions are still host-level; backend already supports `support` staff access on many admin-grade endpoints; frontend still gates the staff console mostly as `admin` only. Recommendation direction: extend the existing staff workspace for managed listings instead of creating a disconnected parallel ops system, and move managed-payment instructions from host profile scope to listing-level settlement profiles with strict access controls. Author signature: (|/) Klaasvaakie
- 2026-05-24: Rechecked `sani.md` against the live code seams before starting. The earlier managed-hosting direction still stands: do not fake this as only a fourth pricing card. Real starting slice should be data model plus access control, because `src/pages/PricingPage.tsx` still hard-codes `standard|professional|premium`, `encore/booking/api.ts` still snapshots host-level payment instructions into bookings, and `src/components/AppRoutes.tsx` still exposes the staff console as admin-only even though the backend already supports `support` on many ops endpoints. Author signature: (|/) Klaasvaakie
- 2026-05-24: Implemented the managed-hosting foundation slice instead of pretending a fourth package is only pricing copy. Added `managementMode` to the shared user contract and listing-level `settlementProfile` to the listing contract, added backend migrations for `identity.users.management_mode` and `catalog.listing_settlement_profiles`, taught booking payment detail lookup to prefer listing-scoped settlement data with legacy host-profile fallback, opened the staff console to `support` users on the frontend, added a dedicated `Managed Hosting` admin section with managed-host/listing coverage and settlement-gap visibility, and extended admin edit flows so staff can set host management mode plus listing-specific payment instructions. Verification: `npx tsx --test tests/api-clients.test.ts` passed, `npm run test:ui -- tests/ui/auth-context.test.tsx tests/ui/dashboard-mobile-shell.test.tsx` passed, and `npm run lint` is still blocked by pre-existing `src/components/HostListingAccessGate.tsx` `Button asChild` typing errors unrelated to this slice. Author signature: (|/) Klaasvaakie
- 2026-05-24: Added the fourth commercial offer to `src/pages/PricingPage.tsx` as a real managed-service card instead of lying with another self-serve checkout. The pricing story now explicitly separates self-service plans from `Managed Hosting`, sells the benefit as “Ideal Stay manages the listing work while the host keeps the upside,” routes the managed CTA to `/signup?role=host&management=managed&returnTo=%2Fpricing`, and updates the FAQ so the site no longer falsely claims Ideal Stay never manages anything. Also updated `src/pages/SignupPage.tsx` to preselect the host role and show a managed-onboarding note when that CTA is used. Verification: `npm run test:ui -- tests/ui/pricing-page.test.tsx tests/ui/signup-page.test.tsx tests/ui/auth-context.test.tsx tests/ui/dashboard-mobile-shell.test.tsx` passed. Author signature: (|/) Klaasvaakie
- 2026-05-24: Fixed the AI trip planner failure mode where upstream Gemini auth/key errors were incorrectly surfaced as client `400` responses. Added provider-auth classification (`lib/server/ai-provider-error.js`), mapped `AiProviderError` to `502` with explicit operator guidance in `api/ai/trip-planner.js` (and parity in `server.ts` AI routes), expanded Gemini key resolution aliases in `lib/server/gemini-config.js` (`SEARCH_AI_GEMINI_API_KEY`, `GEMINI_API_KEY`, `GOOGLE_AI_API_KEY`, `GOOGLE_API_KEY`), enabled DeepSeek fallback on Gemini auth failures in `lib/server/text-generation.js`, and aligned `scripts/check-production-config.mjs` with the same key alias logic so build/runtime expectations match. Added regression coverage in `tests/trip-planner-api.test.ts` and `tests/text-generation.test.ts`. Verification: `npx tsx --test tests/trip-planner-api.test.ts tests/text-generation.test.ts tests/gemini-config.test.ts` passed; `npm run test:ui -- tests/ui/pricing-page.test.tsx tests/ui/signup-page.test.tsx` passed. Author signature: (|/) Klaasvaakie
- 2026-05-24: Verified runtime secret posture and deployment state for trip planner auth errors. `encore/encore.app` exists and points to app id `ideal-stay-online-gh5i`; Encore production has `GEMINI_API_KEY` and `DEEPSEEK_API_KEY` configured. Vercel production also has `SEARCH_AI_GEMINI_API_KEY`, `GEMINI_API_KEY`, and `DEEPSEEK_API_KEY` present by name. Live probe (`POST https://www.idealstay.co.za/api/ai/trip-planner`) still returns `400` with raw Gemini auth-credential error text, confirming production is still running old handler behavior (new `502` guidance patch not deployed yet) and/or active Gemini key restrictions remain invalid. Author signature: (|/) Klaasvaakie
- 2026-05-24: Clarified planner auth layering: the user-facing error text mentioning OAuth is from Google upstream and does not mean Ideal Stay should do Google user login for trip-planner calls. Planner requests should run server-to-server via API key (`SEARCH_AI_GEMINI_API_KEY` / `GEMINI_API_KEY`) plus local anti-abuse controls; current failure indicates provider key validity/restriction or stale deployment, not a product requirement for guest OAuth flow. Author signature: (|/) Klaasvaakie
- 2026-05-24: Hardened AI planner abuse controls and aligned runtime behavior. Removed the accidental `requireAuth: true` gate from `server.ts` `/api/ai/trip-planner` so local/dev and Vercel now share the same contract (guest allowed, rate-limited). Strengthened `lib/server/ai-rails.js` IP extraction to prefer trusted proxy headers (`cf-connecting-ip`, then `x-real-ip`) and ignore invalid spoof-like values before falling back to forwarded chains, reducing rate-limit key evasion noise. Added regression coverage in `tests/trip-planner-api.test.ts` (guest actor path + non-forced auth) and `tests/ai-rails.test.ts` (trusted header precedence + invalid header rejection). Verification: `npx tsx --test tests/ai-rails.test.ts tests/trip-planner-api.test.ts` passed (9/9). Author signature: (|/) Klaasvaakie
- 2026-05-24: Re-verified live production endpoint after the hardening slice. `POST https://www.idealstay.co.za/api/ai/trip-planner` still returns HTTP `400` with the raw upstream Gemini credential error, which confirms production is not yet on the patched handler/runtime path (or still running invalid provider restrictions) even though local tests are green. Author signature: (|/) Klaasvaakie
- 2026-05-24: Deployed the AI hardening slice to Vercel production and Encore remote (`main`), then revalidated live planner behavior. Production moved off the old raw-credential `400` and now returns structured provider `502` responses from the new handler path. Found and fixed an additional runtime bug where DeepSeek fallback reused Gemini model overrides (`SEARCH_AI_MODEL`) and sent invalid model ids to DeepSeek. Patched `lib/server/text-generation.js` to strip Gemini model ids before fallback, updated `lib/server/deepseek-config.js` default model to `deepseek-v4-flash`, expanded regression coverage in `tests/text-generation.test.ts`, and redeployed Vercel production again for the fix. Author signature: (|/) Klaasvaakie
- 2026-05-24: Completed full live deploy/verification pass across both platforms. Vercel production deploy id `dpl_7y9cKtCyagqLggPaZQY2Ajnv6yvm` is aliased to `https://www.idealstay.co.za`; Encore push to remote `encore://ideal-stay-online-gh5i` triggered both staging and prod deploys (`1v4vnc6agip8ehclgqhg`, `1v4vnc6agip8ehclgqig`). Live probe now returns `200` with a planner reply, and abuse control probe as guest showed throttling active (`200, 200, 200, 429, 429, 429`) confirming rate-limit enforcement in production behavior. Author signature: (|/) Klaasvaakie
- 2026-05-25: Fixed planner `400` failures caused by long conversation turns (`AI request text is too long. Max 600 characters.`). Root cause: `validateTripPlannerMessages` rejected any single oversized message, including previous assistant replies, which can naturally exceed 600 chars in normal chat loops. Patched `lib/server/ai-rails.js` so trip-planner message normalization truncates oversized turns to 600 instead of failing the whole request, while keeping existing 12-turn and 4,000-char total caps for abuse control. Added regression coverage in `tests/ai-rails.test.ts` (`validateTripPlannerMessages truncates oversized turns instead of failing`). Verification: `npx tsx --test tests/ai-rails.test.ts tests/trip-planner-api.test.ts` passed. Deployed Vercel prod deployment `dpl_GLforu4QA96D1zSdyZuBKwBH29ue` (aliased to `https://www.idealstay.co.za`) and live-probed `/api/ai/trip-planner` with an 800-char assistant message; endpoint now returns `200` with planner reply instead of `400`. Author signature: (|/) Klaasvaakie
- 2026-05-27: Synced main with upstream by running git pull --no-rebase after fast-forward pull failed due branch divergence (main ahead locally while origin/main moved). Merge completed cleanly (ort), bringing in latest remote updates across booking/ops/content/test seams while preserving existing local working-tree edits (lib/server/ai-rails.js, 	ests/ai-rails.test.ts, sani.md). Current branch state: main is ahead of origin/main by 3 commits (includes the new merge commit plus prior local commits). Author signature: (|/) Klaasvaakie
- 2026-05-27: Production-readiness snapshot run. Evidence: git status shows main ahead of origin/main by 3 with local uncommitted edits in lib/server/ai-rails.js, 	ests/ai-rails.test.ts, and sani.md; 
pm run check:config passed; 
pm run lint failed on src/components/HostListingAccessGate.tsx because Button typing does not accept sChild at multiple call sites (TS2322). Current truth: not production-ready yet; required path is clear lint/type fix + full verification + push/deploy. Author signature: (|/) Klaasvaakie
- 2026-05-27: Production hardening pass to clear release blockers. Replaced unsupported `Button asChild` usage in `src/components/HostListingAccessGate.tsx` with `useNavigate` button actions to satisfy current UI button typings; hardened `src/App.tsx` with safe defaults for `dataErrors`/`hasDataErrors` to prevent null-crash paths in UI tests; aligned README KYC/ops gap wording to the enforced contract test phrase; fixed backend compile blockers by importing `catalogDB` in `encore/booking/api.ts` and adding required `managementMode: "self_service"` in new-user profile payloads inside `encore/identity/api.ts`. Verification: `npm run lint` passed, `npm run test` passed (unit + UI), `npm run build` passed (bundle-budget check passed), and `npx tsc --noEmit` passed in `encore`. Author signature: (|/) Klaasvaakie
- 2026-05-27: Release execution complete. Committed production-gate fixes as `c6a1144` on `main`, pushed to GitHub (`origin/main`) and Encore (`encore/main`), which triggered staging deploy `1v6d2juagip8ehclgvsg` and prod deploy `1v6d2juagip8ehclgvtg` for app `ideal-stay-online-gh5i`. Ran explicit Vercel production deploy and got deployment `dpl_7AzJ9N5Q8c9yR4R3iqrivcJwaKen` (`https://ideal-stay-qlfo5flsr-loop69.vercel.app`) aliased to `https://www.idealstay.co.za`. To avoid shipping unrelated local edits, temporarily stashed `lib/server/ai-rails.js` and `tests/ai-rails.test.ts` during Vercel deploy and restored them immediately after. Author signature: (|/) Klaasvaakie
- 2026-05-27: Completed managed-host CTA end-to-end wiring for `/signup?role=host&management=managed`. Frontend now forwards `managementMode: "managed"` through both email signup and first-time Google signup (`src/pages/SignupPage.tsx`, `src/contexts/AuthContext.tsx`, `src/lib/identity-client.ts`). Backend identity creation now validates and persists public signup management mode in `encore/identity/api.ts` (host-only for managed), instead of hard-coding `self_service`. Added onboarding/admin notification coverage for this path by introducing managed-host notification builders and a paired notifier (`encore/ops/notification-builders.ts`, `encore/ops/notifications.ts`), then triggering it on managed host creation in password and Google signup flows. Added/updated tests: `tests/workflow-client-contracts.test.ts` (client payload includes managementMode), `tests/ui/signup-page.test.tsx` (managed query path submits managed mode), and `tests/backend-notification-helpers.test.ts` (new notification contracts). Verification passed: `npx tsx --test tests/backend-notification-helpers.test.ts tests/workflow-client-contracts.test.ts`, `npm run test:ui -- tests/ui/signup-page.test.tsx`, and `npx tsc --noEmit -p encore/tsconfig.json`. Author signature: (|/) Klaasvaakie
- 2026-05-28: Finished the pending trip-planner hardening in `lib/server/ai-rails.js` and `tests/ai-rails.test.ts` as a shippable slice (not superseded). Re-verified that planner requests still flow through `api/ai/trip-planner.js` -> `lib/server/trip-planner.js` -> `validateTripPlannerMessages(...)`, so per-turn truncation is active behavior on the live route. Kept truncation of oversized turns (600 chars) because it prevents normal multi-turn chats from failing on long assistant replies, while preserving the existing abuse controls (last 12 turns and 4,000 total-character cap). Added missing coverage in `tests/ai-rails.test.ts` for both sides of that contract: truncated oversized turns can pass when aggregate context stays under 4,000, and aggregate context still hard-fails above 4,000. Verification passed: `npx tsx --test tests/ai-rails.test.ts tests/trip-planner-api.test.ts tests/trip-planner.test.ts` (14/14). Author signature: (|/) Klaasvaakie
- 2026-05-28: Investigated host inquiry approve 500s on `/api/encore/bookings/:id/status` and hardened the booking-availability sync seam used by approval. Root issue seam: `replaceBookingAvailabilityBlocks(...)` assumed every booking snapshot produces at least one night (`nights[0]!`), so malformed/zero-night rows could crash sync and bubble as internal errors during host approval. Patched `encore/catalog/api.ts` to normalize check-in/out keys, skip non-positive stay windows with explicit warning logs, and continue syncing valid holds/bookings instead of crashing the request. This keeps operational approvals moving while preserving existing overlap/manual-block guards for valid rows. Verification passed: `npx tsx --test tests/catalog-availability.test.ts tests/booking-workflow.test.ts tests/api-clients.test.ts` (33/33). Author signature: (|/) Klaasvaakie
- 2026-05-28: Reworked referral rewards away from cashback to platform-growth benefits. Updated `encore/referrals/api.ts` so subscription referral rewards now represent non-cash units (`host`: ad credits/promo benefits tiers 25/40/60, `guest`: holiday draw entries tiers 1/2/3) and no longer mutate wallet `balance` as payout money; referral count/tier progression still advances on real conversion. Updated referral reward notifications (`encore/ops/notification-builders.ts`, `encore/ops/notifications.ts`) to message ad credits/promotion benefits or holiday-draw entries instead of rand cashback. Updated referral UI copy and reward-value rendering in `src/pages/ReferralView.tsx` to remove cashback language and show unit-based rewards. Verified with `npx tsx --test tests/backend-notification-helpers.test.ts tests/workflow-client-contracts.test.ts tests/api-clients.test.ts` (33/33). Author signature: (|/) Klaasvaakie
- 2026-05-28: Added pragmatic realtime refresh behavior for host workflow surfaces without forcing manual page refreshes. `src/hooks/use-platform-data.ts` now runs an authenticated booking/host-data sync tick every 12 seconds and also triggers immediate sync on tab focus/visibility changes; this keeps Host Dashboard cards, Host Enquiries queues, and related booking-state modals aligned with backend transitions even when actions happen in another tab/session. Existing optimistic per-action updates remain in place, and background sync serves as reconciliation/freshness layer. Verification passed: `npm run test:ui -- tests/ui/host-dashboard.test.tsx tests/ui/host-enquiries.test.tsx` (7/7). Author signature: (|/) Klaasvaakie
- 2026-05-28: Fixed flaky e2e booking lifecycle selector collisions in `tests/e2e/booking-payment-review.spec.ts` caused by two visible month grids exposing duplicate `data-day` buttons in the date-range picker. Updated both date clicks to deterministic `.first()` targets so strict-mode Playwright selection no longer fails on duplicate day nodes. Verification passed: `npx playwright test tests/e2e/booking-payment-review.spec.ts --reporter=line` (1/1). Author signature: (|/) Klaasvaakie
- 2026-05-28: Fixed marketplace availability mismatch that could still allow enquiries on already booked dates when `availabilityBlocks` existed but lagged behind `blockedDates`. Root seam was `src/lib/listing-availability.ts` preferring `availabilityBlocks` and ignoring `blockedDates` entirely in that case. Patched to union both sources so canonical blocked nights include any booked/held/manual dates from either payload surface. This closes the overlap leak on listing-detail calendar disable logic and explore-date filtering. Verification passed: `npx tsx --test tests/catalog-availability.test.ts tests/host-availability.test.ts tests/api-clients.test.ts` (33/33) and `npx playwright test tests/e2e/booking-payment-review.spec.ts --reporter=line` (1/1). Author signature: (|/) Klaasvaakie
- 2026-06-04: Split the payment/Yoco contract coverage into a dedicated `tests/payment-yoco-contracts.test.ts` file, kept the client contract checks there, and removed the obsolete Yoco setup expectations from `tests/ui/host-dashboard.test.tsx` because `HostDashboard` no longer owns billing-card setup. Verification passed: `npm test -- --test tests/payment-yoco-contracts.test.ts tests/api-clients.test.ts` and `npm run test:ui -- tests/ui/pricing-page.test.tsx tests/ui/host-dashboard.test.tsx`. Author signature: (|/) Klaasvaakie
- 2026-06-04: Moved host Subscription Management out of the Host Overview and into Administration -> Account. Extracted the billing UI/return handling into `src/components/HostSubscriptionManagement.tsx`, rendered it inside `src/pages/AccountPage.tsx`, added an Administration sidebar item for `Subscription`, changed host billing setup return URLs from `/host` to `/account`, and updated dashboard/account UI tests to prove the block is no longer on overview but still starts Yoco setup from Account. Verification passed: `npm run lint` and `npm run test:ui -- tests/ui/host-dashboard.test.tsx tests/ui/account-page.test.tsx tests/ui/auth-context.test.tsx`. Author signature: (|/) Klaasvaakie
- 2026-06-04: Checked Encore CLI permissions and linkage. `encore auth whoami` is authenticated, `encore.app` now links to `ideal-stay-online-gh5i`, and the CLI surface still has no direct deploy command. Available commands are app/auth/build/check/config/daemon/db/exec/gen/llm-rules/logs/mcp/namespace/rand/run/secret/telemetry/test. The real deploy path needs Encore Cloud or MCP, not wishful thinking. Author signature: (|/) Klaasvaakie
- 2026-06-04: Started the local Encore MCP server with `encore mcp start --app ideal-stay-online-gh5i`. It exposes `http://localhost:9900/sse?app=ideal-stay-online-gh5i`, which confirms the CLI can host an MCP session, but this chat session still does not expose an Encore MCP tool to drive cloud actions directly. Author signature: (|/) Klaasvaakie
- 2026-06-04: Deployed the current billing/Yoco changes by committing `971797a` and pushing `main` to the `encore` remote. Encore confirmed both staging and prod deploy triggers for app `ideal-stay-online-gh5i`: staging deploy `1vbunjeagip2nm01jgrg` and prod deploy `1vbunjeagip2nm01jgs0`. Author signature: (|/) Klaasvaakie
- 2026-06-04: Added `docs/encore-deployment.md` to explain the real deployment flow in this repo: local tests, commit, `git push encore main`, Encore Cloud deploy triggers, the role of the linked app `ideal-stay-online-gh5i`, and the difference between deployment and MCP inspection. Author signature: (|/) Klaasvaakie
- 2026-06-04: Clarified that the frontend deploys separately through Vercel and can auto-deploy from GitHub commits because the repo is Vercel-linked (`.vercel/project.json`, `vercel.json`). Updated `docs/encore-deployment.md` to state that `git push encore main` triggers Encore backend deploys while `git push origin main` can trigger the Vercel frontend pipeline. Author signature: (|/) Klaasvaakie
- 2026-06-04: Added a temporary Yoco test-mode banner to the pricing page, controlled by `VITE_YOCO_PAYMENT_MODE=test`, and documented that it should be flipped back to live after testing. Verified with `npm run lint` and `npx vitest run tests/ui/pricing-page.test.tsx`. Author signature: (|/) Klaasvaakie
- 2026-06-04: Committed the temporary Yoco test-mode banner as `c806215` (`Add Yoco test mode banner`) so the pricing page can visibly signal test mode while the checkout flow is being validated. Author signature: (|/) Klaasvaakie
- 2026-06-04: Cleaned up the browser audit warning for missing form attributes by adding stable `id`/`name` values to the hidden profile photo upload in `src/pages/AccountPage.tsx` and the KYC ID/selfie file inputs in `src/components/KYCModal.tsx`. The CSP inline-script/style warning and the deprecated Protected Audience API notice appear to be external/browser-policy issues, not our form markup. Author signature: (|/) Klaasvaakie
- 2026-06-04: Deployed the latest local fixes by committing `9a5bd51` (`Fix form attributes and notes`) and pushing it to both `origin/main` and `encore/main`. GitHub updated successfully, and Encore triggered staging deploy `1vbv0teagip2nm01jgv0` plus prod deploy `1vbv0teagip2nm01jgvg` for app `ideal-stay-online-gh5i`. Author signature: (|/) Klaasvaakie
- 2026-06-05: Fixed the host drift that sent Yoco test users back to a Vercel preview URL instead of the canonical production alias. Added a frontend redirect in `src/App.tsx` that forces any non-local `*.vercel.app` host over to `https://www.idealstay.co.za`, then committed as `8d66b40` (`Force canonical production host`) and pushed it to both `origin/main` and `encore/main`. Encore triggered staging deploy `1vcc6amagipe20bg2ga0` and prod deploy `1vcc6amagipe20bg2gag`. Author signature: (|/) Klaasvaakie
- 2026-06-05: Removed the runtime Vercel host redirect and the remaining `vercel.app` allowlist entries from Encore app config, then forced Yoco checkout mode to default to `test` when no explicit mode is configured. Committed as `a7892c3` (`Remove vercel host refs and default Yoco test mode`) and pushed to `origin/main` plus `encore/main`. Encore triggered staging deploy `1vccosuagipe20bg2gbg` and prod deploy `1vccosuagipe20bg2gc0`. This is the temporary testing posture and should be flipped back to live when testing is done. Author signature: (|/) Klaasvaakie
- 2026-06-05: Observed a real checkout failure in Yoco hosted checkout: `Three D authentication failed.` Console noise from LaunchDarkly, Google Pay, Cardinal, and CSP inline-event warnings is not the root cause. Repo-side config is already set for Yoco test mode, so the current blocker is provider-side checkout/auth behavior, not our payment initiation code. Author signature: (|/) Klaasvaakie
- 2026-06-05: Reset Encore secrets so the cloud app is explicitly in Yoco test mode and uses the canonical frontend URL. Deleted the old `YOCO_PAYMENT_MODE`, `YOCO_TEST_SECRET_KEY`, and `IDEAL_STAY_APP_URL` secret groups that had conflicting environment coverage, then recreated `YOCO_PAYMENT_MODE=test` and `YOCO_TEST_SECRET_KEY=sk_test_960bfde0VBrLlpK098e4ffeb53e1` for `type:development, type:local, type:preview, type:production`, and `IDEAL_STAY_APP_URL=https://www.idealstay.co.za` for `type:preview, type:production`. Author signature: (|/) Klaasvaakie
- 2026-06-05: Deployed the latest Yoco test-state note update by committing `e4b25da` (`Record Yoco test deployment state`) and pushing it to `origin/main` plus `encore/main`. GitHub updated successfully, and Encore triggered staging deploy `1vcifveagipe20bg2gs0` and prod deploy `1vcifveagipe20bg2gsg`. Author signature: (|/) Klaasvaakie
- 2026-06-05: Removed the Vercel analytics dependency from `src/main.tsx` and restored a canonical-host redirect so any non-local host gets sent to `https://www.idealstay.co.za` before the app renders. This keeps checkout return traffic on the real domain instead of letting a stale host leak through. Verification: `npm run lint` passed and a source scan shows no `vercel` references left under `src` or `encore`. Author signature: (|/) Klaasvaakie
- 2026-06-05: Standardized active Yoco payment creation on `POST /billing/payments`. Removed the old exposed backend purchase endpoints for subscription checkout, host setup checkout, and content credit checkout; the frontend pricing page now starts subscriptions through `startBillingPayment({ purpose: "subscription" })`, while managed hosting still wraps the same endpoint. Added migration `12_standard_payment_purpose_constraints.up.sql` so persisted payment purposes include `managed_hosting`, and managed-hosting payment fulfillment now marks the user as `management_mode='managed'` with an active paid billing account instead of pretending it is only card setup. Verification passed: `npm run lint`, `npm test -- --test tests/payment-yoco-contracts.test.ts tests/workflow-client-contracts.test.ts`, and a clean rerun of `npx vitest run tests/ui/social-dashboard.test.tsx`; the combined Vitest run also passed inside the broad test command. Author signature: (|/) Klaasvaakie
- 2026-06-05: Investigated payment `64d608fc-509e-44fa-9c81-f06d6df5c9fb` returning to `https://ideal-stay.vercel.app`. The payment intent was not in prod `billing-db`; it existed in staging as a pending subscription payment, proving production traffic was still using the staging Encore backend. Reset Encore `IDEAL_STAY_APP_URL` for staging plus prod/preview to `https://www.idealstay.co.za`, replaced Vercel production `ENCORE_API_URL` with the production Encore host, and removed Vercel production `ALLOW_STAGING_ENCORE_BACKEND`. Author signature: (|/) Klaasvaakie
- 2026-06-05: Redeployed Vercel production after the backend env fix. First deploy attempt hit a transient `ECONNRESET`; retry succeeded with deployment `dpl_88NJWMWSGitDBT5EVrmzpcGVvThx`, production URL `https://ideal-stay-mz7ydh3kq-loop69.vercel.app`, and alias `https://www.idealstay.co.za`. Author signature: (|/) Klaasvaakie
- 2026-06-05: Reverted the deployment topology intentionally after user clarified Encore production is not ready and staging must remain the active test backend. Vercel production `ENCORE_API_URL` now points to the staging Encore host and Vercel production has `ALLOW_STAGING_ENCORE_BACKEND=true`; Encore `IDEAL_STAY_APP_URL` remains canonical so Yoco returns to `https://www.idealstay.co.za` while payment records are written to staging. Redeployed Vercel production as `dpl_D1dV8CT4BV7WZmc5q5Mg2yeM5fki`, aliased to `https://www.idealstay.co.za`, and verified `/api/encore/billing/plans` responds. Author signature: (|/) Klaasvaakie
- 2026-06-05: Investigated successful-return payment `0bd7420d-3220-4615-b6a5-efbbda5df78a`. It exists in staging `billing_payment_intents` as a `pending` premium monthly subscription with no matching `billing_webhook_events`, so the checkout returned successfully but Yoco test mode did not deliver a webhook that the backend could reconcile. Added a narrow test-mode-only fallback: the frontend now passes `billingStatus=success` to `GET /billing/payments/:paymentId`, and the backend only fulfills from that return signal when `provider_mode='test'`. Live mode still requires webhook/provider proof. Verification passed: `npm run lint`, payment/workflow contract tests, and pricing UI test. Author signature: (|/) Klaasvaakie
- 2026-06-05: Manually repaired staging payment `0bd7420d-3220-4615-b6a5-efbbda5df78a` for user `c19a9994-2a7c-4ca6-b662-ed6e0601a918` (`loop69org@gmail.com`) after it returned success before the test fallback was deployed. Marked the payment paid, created active premium monthly subscription `00f8b9be-7f44-4287-a0cd-6026a1e49172`, updated the identity user `host_plan` to `premium`, and synced `host_billing_accounts` to `premium/paid/active/card_on_file=true`. Also fixed `syncPaidBillingAccount` so existing accounts update `card_on_file=true` during paid subscription activation. Verification passed: staging DB readback, `npm run lint`, and payment/workflow contract tests. Author signature: (|/) Klaasvaakie
- 2026-06-05: Synced Encore production data to match staging across all app databases using admin DB access. Copied non-migration table data from staging to prod for `identity-db`, `catalog-db`, `booking-db`, `billing-db`, `messaging-db`, `ops-db`, `reviews-db`, `referrals-db`, and `analytics-db`, preserving IDs and resetting sequences. Independent verification showed every copied prod table count equals staging, and live probes confirmed staging/prod parity for `/listings` and `/billing/plans`. Current frontend cutover remains intentionally pointed at Encore staging until the user decides to switch Vercel `ENCORE_API_URL` to the prod Encore URL. Author signature: (|/) Klaasvaakie
- 2026-06-05: Analysed the app route/service/test surface and tightened workflow coverage. Added route ownership coverage to `tests/workflow-coverage.test.ts`, expanded `tests/fixtures/workflows.ts` and `docs/workflow-validation-matrix.md` so `/host/quick-replies`, `/host/reports`, and all public legal routes are owned by workflows, added `tests/ui/app-routes.test.tsx` to prove legal pages render publicly, and restored the README KYC audit-history wording required by the contract test. Verification passed: `npm run lint` and `npm test` (173 unit tests, 43 UI tests; live smoke skipped by script). Author signature: (|/) Klaasvaakie
- 2026-06-06: Created inspectable payment-flow diagrams for the current canonical Yoco flow. Added `docs/payment-flow-diagrams.md` plus standalone Mermaid and rendered SVG files under `docs/diagrams/payment/` covering system context, start-payment sequence, purpose routing, canonical payment-intent data model, return/status resolution, webhook reconciliation, fulfillment by purpose, test-mode fallback, and runtime config boundaries. Verified Mermaid syntax by rendering all eight `.mmd` files with Mermaid CLI 11.15.0. Author signature: (|/) Klaasvaakie
- 2026-06-06: Root-caused paid Yoco subscription intents not becoming active subscriptions. Yoco Checkout payment webhooks expose the provider checkout reference as `payload.metadata.checkoutId`; the billing webhook matcher only checked `metadata.paymentIntentId` and `payload.id`, so real `payment.succeeded` events could be stored but fail to match `billing_payment_intents.provider_checkout_id`. Patched webhook matching and delayed status reconciliation to use `payload.metadata.checkoutId`, added contract coverage in `tests/payment-yoco-contracts.test.ts`, updated the payment diagrams, and manually repaired active staging user `snymanella404@gmail.com` by marking payment intent `829c646f-82d7-4f39-bfd1-03b442f36d38` paid, creating active premium subscription `1996db18-a63c-4a89-922d-b88dc05f499b`, and syncing `host_billing_accounts` to `premium/paid/active/card_on_file=true` through 2026-07-05. Verification passed: DB readback, `npm run lint`, `npx tsx --test tests/payment-yoco-contracts.test.ts`, and `npm test` (174 unit tests, 43 UI tests; live smoke skipped). Author signature: (|/) Klaasvaakie
- 2026-06-08: Turned `/host/onboarding` from a static checklist into a real setup gate. `HostOnboardingTutorial` now receives the current host profile and host listings from `AppRoutes`, reads saved host quick replies from `/messages/quick-replies/me`, and computes complete/missing state for account/KYC, payment setup, first listing, availability, quick replies, and enquiry readiness. Payment guidance now branches correctly: self-service hosts still use `/account` profile payment instructions, while managed hosts are guided to listing-scoped settlement setup through `/host/listings` or listing creation, avoiding the loose account banking field. Added UI coverage for standard hosts, managed hosts with complete listing settlement setup, and managed hosts missing listing settlement profiles. Verification passed: `npm run lint` and `npm run test:ui -- tests/ui/host-onboarding-tutorial.test.tsx`. Author signature: (|/) Klaasvaakie
- 2026-06-08: Deployed commit `566992d` (`Ship host onboarding and admin billing fixes`). Pre-deploy verification passed: `npm run lint`, `npx tsc --noEmit -p encore/tsconfig.json`, `npm test` (194 unit tests and 51 UI tests), and `npm run build`. Pushed `main` to GitHub `origin` and Encore remote `encore`, triggering Encore staging deploy `1vegsimagip74obdouvg` and prod deploy `1vegsimagip74obdov00`. Ran Vercel production deploy `dpl_CTXynSsB84m9zwhsouH2yqQxzAnT`, which finished `READY` and aliased `https://www.idealstay.co.za`. Live probes passed: homepage `200`, anonymous `/api/encore/auth/session` `401`, and `/api/encore/listings?status=active` `200`. Author signature: (|/) Klaasvaakie
- 2026-06-08: Removed the visible `( |/ ) Klaasvaakie` signature from the host onboarding tutorial while keeping the author marker as a source-code comment only. Updated `HostDashboard` so the new-host tutorial button/card disappear once the real onboarding setup is complete: verified profile/KYC, self-service account payment instructions or managed listing settlement profiles, at least one listing, availability touched for every listing, and all saved quick replies present. The `/host/onboarding` route remains available from the host navigation for hosts who want to redo it. Verification passed: `npm run test:ui -- tests/ui/host-onboarding-tutorial.test.tsx tests/ui/host-dashboard.test.tsx` and `npm run lint`. Author signature: (|/) Klaasvaakie
- 2026-06-10: Tightened subscription fulfillment so a successful paid checkout cannot leave stale subscription data behind on replay. `activatePlanFromBillingSession` now updates an existing subscription row for the same checkout session instead of only inserting on the first pass, and it still cancels any other active subscriptions for the user. Added a regression guard in `tests/payment-yoco-contracts.test.ts` for the existing-subscription update branch. Verification pending. Author signature: (|/) Klaasvaakie
- 2026-06-10: Repaired a corrupted `src/components/ListingDetail.tsx` build blocker by restoring the file from `HEAD`. Normalized catalog admin-tag mapping in `encore/catalog/api.ts` so raw strings cannot leak into the typed listing contract, then removed an unused admin tag import in `src/features/admin/dashboard-support.tsx`. Verification passed: `npm run lint`, `npm run build`, and `npx tsx --test tests/payment-yoco-contracts.test.ts`. Live DB trace was attempted via Encore DB tooling but blocked because this machine has no `psql` client and Docker is unavailable for Encore's fallback shell. Author signature: (|/) Klaasvaakie
- 2026-06-10: With Docker available, used a disposable `postgres:16-alpine` client container against the Encore proxy to inspect the live prod billing rows. The current paid user `c19a9994-2a7c-4ca6-b662-ed6e0601a918` (`loop69org@gmail.com`) has `host_plan = premium` in identity, and the billing DB shows the latest active subscription row as `premium` with matching `billing_interval = monthly`. Older pending rows still exist from earlier attempts, but a mismatch query between active subscriptions and identity host plans returned no rows. Author signature: (|/) Klaasvaakie
- 2026-06-10: Cleaned the prod Encore billing history by cancelling 11 stale `billing_payment_intents` rows that were still `pending` from earlier checkout attempts. Used the write-enabled Encore DB proxy and a disposable PostgreSQL client container. After cleanup, `c19a9994-2a7c-4ca6-b662-ed6e0601a918` has 0 pending intents, 1 paid intent, and 10 cancelled intents; `c81a6158-019f-44cb-b57f-fe284e1680ce` has 1 cancelled intent and no pending intents. The paid subscription row remained untouched. Author signature: (|/) Klaasvaakie
- 2026-06-10: Removed the remaining stale billing noise by deleting 19 orphaned `billing_webhook_events` rows in prod. All were `yoco/payment.succeeded`, all were unprocessed, and none linked to any `billing_payment_intents` row by `paymentIntentId` or `checkoutId`, so they were safe to drop. Final verification showed `billing_webhook_events` count is 0 and no orphaned Yoco webhook rows remain. Author signature: (|/) Klaasvaakie
- 2026-06-10: Ran a targeted stale-code sweep with `knip` and `rg`. Most hits were intentional guardrails or live contracts, but the repo still has unrelated dirty worktree changes in catalog/listing/tag surfaces (`encore/catalog/api.ts`, `encore/catalog/migrations/1_init.up.sql`, `encore/shared/domain.ts`, `src/lib/domain-mappers.ts`, `src/types.ts`, plus new tag-related files). I did not touch those because they are outside the billing cleanup path and should be reviewed as a separate change set. Author signature: (|/) Klaasvaakie
- 2026-06-10: Finished the listing admin tag surface. Public cards already showed the tag; I added the same badge/message to `src/components/ListingDetail.tsx`, and updated `tests/api-clients.test.ts` so the canonical Encore listing contract expects `adminTagKey`/`adminTagNote` in the payload. Verification passed on the targeted run: `npm test -- --runInBand tests\\api-clients.test.ts tests\\ui\\host-onboarding-tutorial.test.tsx tests\\ui\\app-routes.test.tsx` and the full UI suite still passed. Author signature: (|/) Klaasvaakie
# 2026-06-10 scan log

- Ran a broad dead-code/dependency scan from the repo root.
- `knip --include files --no-progress` returned clean.
- Searched declared dependencies against source imports; no clearly unused installed packages surfaced.
- Found tracked runtime artifacts under `test-results/` in git status, but left them alone because they are likely local test outputs and not part of the current user change set.
- Repo already has unrelated modified files and new untracked files; I did not touch them.

# 2026-06-10 deeper scan

- Removed two confirmed unused dependencies: `@hookform/resolvers` and `@vercel/analytics`.
- Re-ran `knip --reporter json`; it now reports only CSS/framework false positives for some deps plus several unused exports in shared UI and client helper modules.
- Verified the typecheck still passes with `npm run lint:types`.
- The remaining stale-code candidates are export-level helpers like `CardAction`, `DialogClose`, `DialogOverlay`, `DialogPortal`, `DialogTrigger`, `PopoverDescription`, `PopoverHeader`, `PopoverTitle`, `SelectGroup`, `SelectLabel`, `SelectSeparator`, `SelectScrollUpButton`, `SelectScrollDownButton`, `CalendarDayButton`, `HealthMetric`, `buildBookingReturnPath`, `requestProfilePhotoUpload`, `uploadChatAttachment`, and `encoreFetch`; they are still present but not imported anywhere outside their defining files.
- Author signature: (|/) Klaasvaakie

# 2026-06-10 dead-export cleanup

- Removed unused UI exports from `src/components/ui/card.tsx`, `dialog.tsx`, `popover.tsx`, `select.tsx`, and `calendar.tsx`.
- Demoted `HealthMetric` in `src/features/admin/dashboard-support.tsx` to an internal helper.
- Demoted `buildBookingReturnPath` in `src/lib/booking-auth-intent.ts` and `encoreFetch` in `src/lib/encore-client.ts` to internal helpers.
- Deleted dead client-only helpers `requestProfilePhotoUpload` from `src/lib/identity-client.ts` and `uploadChatAttachment` from `src/lib/media-client.ts`.
- Verified `npm run lint:types`, `npm test`, and `npm run build` all passed after the cleanup.
- Author signature: (|/) Klaasvaakie

# 2026-06-10 follow-up cleanup

- Removed `listAdminHostBillingAccounts` from `src/lib/billing-client.ts` after confirming it was not imported anywhere.
- Demoted `normalizeDateOnly` and `getInquiryStateLabel` in `encore/booking/workflow.ts` to internal helpers.
- Converted the exported Zod schema objects in `src/lib/domain-mappers.ts` to internal constants while keeping the public parse helpers intact.
- Re-verified with `npm run lint:types`, `npm test`, and `npm run build`; all passed.
- `knip` now only reports expected CSS/framework dependency false positives and still-unused exported helper types/values that are part of shared API surfaces, not obvious dead code.
- Author signature: (|/) Klaasvaakie

# 2026-06-10 final cleanup pass

- Removed `getInquiryDisplayState`, `getInquiryDeclineReasonLabel`, `getInquiryResponseText`, and `canGuestViewStayDetails` from the public surface of `src/lib/inquiry-state.ts`; they remain internal helpers for the same module.
- Removed the dead `notifyInquiryApproved` export from `encore/ops/notifications.ts`.
- Verified the client-side `src/lib/listing-tags.ts` is still imported by `AdminDashboard`, `PropertyCard`, and `ListingDetail`, so it stays in place.
- Re-ran `knip`, `npm run lint:types`, `npm test`, and `npm run build`; all passed.
- The remaining `knip` hits are framework/CSS dependency false positives plus intentional shared API exports that are still in use.
- Author signature: (|/) Klaasvaakie

# 2026-06-10 deploy

- Committed the verified cleanup as `9241e10` with message `Prune dead exports and unused deps`.
- Pushed `main` to `origin` so the GitHub-linked Vercel deployment can pick it up.
- Pushed `main` to `encore`, which triggered Encore Cloud deploys for staging and prod:
  - `https://app.encore.cloud/ideal-stay-online-gh5i/envs/staging/deploys/1vft296agip74obdp1bg`
  - `https://app.encore.cloud/ideal-stay-online-gh5i/envs/prod/deploys/1vft296agip74obdp1c0`
- Author signature: (|/) Klaasvaakie

# 2026-06-22 vercel account check

- Confirmed this checkout is linked locally to Vercel project `prj_komlJxxNhZdzCD7PyxNestigAO5A` with org/team `team_DAsOov3nTllu6SluJSqvpf0X` and project name `ideal-stay`.
- `vercel.json` still routes frontend traffic to `/api/encore-proxy` for Encore API calls, so the app wiring itself is unchanged.
- The `.vercel` folder is local-only and should stay uncommitted; it contains the current Vercel link metadata and environment files.
- Author signature: (|/) Klaasvaakie

# 2026-06-22 relinked to new vercel home

- Relinked this checkout to `loop69/idealtrue` with project ID `prj_ehouhIGWLvaGCPKow4a8zH0Jfbjs`.
- Verified via `vercel project inspect idealtrue --scope loop69`; root directory is `.`, framework preset is Vite, and the project is owned by `Loop69`.
- `vercel link` surfaced both `origin` and `encore` remotes, but the actual Vercel binding is now the new team project the user asked for.
- Author signature: (|/) Klaasvaakie

# 2026-06-22 ownership blocker

- The active Vercel CLI identity is `looping69`, and `vercel teams ls` only shows the `loop69` scope in this session.
- There is no visible `klaasvaakie` scope available here, so the project cannot be moved to that owner without re-authenticating or being granted access to the target scope.
- Author signature: (|/) Klaasvaakie

# 2026-06-22 vercel logout

- Logged out of the Vercel CLI successfully.
- A follow-up `vercel teams ls` now reports no existing credentials and starts the device-login flow, so the old `looping69` session is cleared.
- Author signature: (|/) Klaasvaakie

# 2026-06-22 handoff for klaasvaakie login

- The next step is to log the Vercel CLI into the `klaasvaakie` account and then relink this checkout to that scope.
- After login, the relink command should target the `klaasvaakie` scope/project, not `loop69`.
- Author signature: (|/) Klaasvaakie

# 2026-06-22 deployment wiring check

- Confirmed the repo still has the expected release surfaces wired together: `origin` points at GitHub, `encore` points at `encore://ideal-stay-online-gh5i`, and the local Vercel project binding exists for `idealtrue`.
- Confirmed the Encore app ID is `ideal-stay-online-gh5i` in `encore.app`, and the frontend Vercel config still rewrites `/api/encore/*` to the same-origin proxy.
- Live deployment health still needs authenticated checks against GitHub Actions, Vercel, and Encore dashboards or CLIs; the repo files alone prove wiring, not current cloud-side status.
- Author signature: (|/) Klaasvaakie

# 2026-06-22 live deployment check

- GitHub Actions auth is present for `WimpyvL`, and the latest visible `Staging Smoke` run on `main` failed (`27083093728`).
- Vercel and Encore still need cloud-side verification; no live production URL or authenticated dashboard access was available in this session for a definitive health check.
- Author signature: (|/) Klaasvaakie

# 2026-06-22 vercel verified

- Authenticated Vercel with the provided token and relinked the checkout to `klaasvaakies-projects/idealtrue`.
- Verified the production deployment is `Ready` at `https://idealtrue-qvsouc2t3-klaasvaakies-projects.vercel.app` with aliases including `https://idealtrue.vercel.app`.
- Verified the required Vercel env vars exist for production/preview: `ENCORE_API_URL`, `YOCO_SECRET_KEY`, `YOCO_WEBHOOK_SECRET`, `VITE_GOOGLE_CLIENT_ID`, `VITE_GOOGLE_MAPS_API_KEY`, `SEARCH_AI_GEMINI_API_KEY`, `SEARCH_AI_MODEL`, and `ALLOW_STAGING_ENCORE_BACKEND`.
- Author signature: (|/) Klaasvaakie

# 2026-06-23 backend proxy proof

- Probed `https://idealtrue.vercel.app/api/encore/auth/session` and got `401 unauthenticated`, which shows the Vercel proxy is actually reaching Encore.
- Probed `https://idealtrue.vercel.app/api/encore/listings?status=active` and got `200` with live listing JSON, which is stronger proof that the frontend proxy is talking to the backend successfully.
- `https://idealtrue.vercel.app/api/encore/health` returned a backend `404`, so that path is not a valid health signal here and should not be used as proof of backend failure.
- Author signature: (|/) Klaasvaakie

# 2026-06-23 screenshot diagnosis

- The browser screenshot showed a frontend banner saying `Listing response was invalid`, but the live `www.idealstay.co.za/api/encore/listings?status=active` endpoint currently returns valid JSON and `200`.
- That makes the screenshot most likely stale browser state, a prior deployment, or a transient frontend contract failure that is no longer present.
- Author signature: (|/) Klaasvaakie

# 2026-06-23 root cause and fix

- Found the real breakage: one live public listing had `location: ""`, which violated the listing contract and caused `listPublicListings()` to fail the entire homepage.
- Hardened `src/lib/platform-client.ts` so public listings now skip malformed records instead of blanking the whole marketplace.
- Added a regression test proving one bad listing no longer takes down the homepage.
- `npm test -- --runInBand tests/api-contract-validation.test.ts` now passes; `npm run lint` still fails on unrelated pre-existing unused-export noise in other files.
- Author signature: (|/) Klaasvaakie

# 2026-07-06 yoco webhook hardening

- Hardened `encore/billing/api.ts` webhook processing so a Yoco callback now rejects metadata ownership mismatches before subscription activation or payment fulfillment.
- Kept the existing payment-intent lookup flow intact, but added a strict user ownership check against `metadata.userId` before a payment intent can be fulfilled.
- Added a regression assertion in `tests/payment-yoco-contracts.test.ts` covering the ownership guard and verified it with `npx tsx --test tests/payment-yoco-contracts.test.ts` plus `npx tsc --noEmit -p encore/tsconfig.json`.
- Author signature: (|/) Klaasvaakie

# 2026-07-06 webhook event filtering and idempotency

- Tightened the Yoco webhook handler so only `paid`, `failed`, and `cancelled` outcomes can touch billing state; anything else now exits early with a `202 ignored` response.
- Added a regression assertion for the non-fulfilment early-return path and a duplicate-delivery guard assertion keyed off `billing_webhook_events.id`.
- Re-ran `npx tsx --test tests/payment-yoco-contracts.test.ts` and `npx tsc --noEmit -p encore/tsconfig.json`; both passed.
- Author signature: (|/) Klaasvaakie

# 2026-07-06 subscription management dashboard

- Added a new admin dashboard subscriptions section that lists active subscription rows with the exact subscription ID, user, plan, billing interval, amounts, and start/end dates.
- Wired per-row actions for cancel and upgrade. Cancel now hits a dedicated subscription-id endpoint; upgrade opens a Yoco checkout for the exact subscription owner and carries the source subscription ID through payment metadata.
- Extended the frontend subscription model with `billingInterval` so the dashboard and admin client can render the real billing cadence.
- Verified with `npx tsc --noEmit -p encore/tsconfig.json`, `npm run lint:types`, and `npm run test:ui -- tests/ui/admin-dashboard-data.test.tsx tests/ui/admin-financials.test.tsx`.
- Author signature: (|/) Klaasvaakie

# 2026-07-06 encore compile fix

- Replaced the Encore API generic response type that referenced `Awaited<ReturnType<...>>` with a concrete `AdminSubscriptionUpgradePayment` shape.
- Kept the upgrade endpoint returning the existing payment object, which already includes the provider mode and checkout metadata needed by the dashboard.
- Re-ran `npx tsc --noEmit -p encore/tsconfig.json` and `npx tsx --test tests/payment-yoco-contracts.test.ts`; both passed after the fix.
- Author signature: (|/) Klaasvaakie

# 2026-07-06 payment and subscription trace

- Mapped the live subscription payment path from `src/pages/PricingPage.tsx` through `src/lib/billing-client.ts`, `encore/billing/api.ts`, `encore/billing/payment-return.ts`, the Yoco webhook handler, `subscriptions`, `users.host_plan`, and `host_billing_accounts`.
- Confirmed that actual subscription activation only happens inside `fulfilSuccessfulPaymentIntent()` -> `activatePlanFromBillingSession()`, which inserts or updates `subscriptions`, updates `users.host_plan`, and syncs `host_billing_accounts`.
- Isolated the break: live browser return reconciliation cannot mark a payment intent paid unless a successful webhook already exists or `provider_order_id` is already stored, but `provider_order_id` is only captured from the webhook path. If the webhook does not land, the app keeps the payment intent pending and never activates the subscription.
- No code changes made in this pass; this was a direct code-path map and root-cause finding.
- Author signature: (|/) Klaasvaakie

# 2026-07-06 subscription activation and dashboard return fix

- Added direct Yoco checkout verification to pending payment-intent reconciliation so live subscription activation no longer depends on the webhook being the only place that reveals payment completion.
- Kept webhook fulfilment intact, but added a checkout-status fallback using the stored `provider_checkout_id`, then routed successful returns by payment purpose instead of forcing everything back through `/pricing`.
- Added a host-dashboard `Subscriptions` modal under the `Administration` group, backed by `GET /billing/subscriptions`, and changed successful subscription returns to land on `/host?modal=subscriptions`.
- Updated payment contract tests and the host billing E2E fixture expectations to match the new return path; Encore typecheck, frontend typecheck, and `tests/payment-yoco-contracts.test.ts` passed. Playwright browser binaries were missing locally, so the E2E spec itself could not be executed to completion in this environment.
- Author signature: (|/) Klaasvaakie

# 2026-07-06 managed hosting status surfaced in host subscriptions modal

- Traced the mismatch: managed hosting payments activate `users.management_mode = 'managed'` and keep the underlying host plan on `premium`, but the host subscriptions modal was only reading `subscriptions` rows.
- Updated the host subscriptions modal to load the host billing account as well, detect managed hosts from the live profile, and surface `Managed Hosting` as the current access state instead of pretending everything must appear as a standard subscription row.
- Added an explicit managed-hosting explanation block in the modal so the user sees the paid managed lane, billing status, and underlying premium plan together.
- Re-ran `npm run lint:types` and `npx tsc --noEmit -p encore/tsconfig.json`; both passed.
- Author signature: (|/) Klaasvaakie

# 2026-07-06 managed listings surfaced in admin listing management

- Traced the admin blind spot to `ListingsSection`: the table rendered host and status data, but it never marked a listing as managed even when its owning host had `managementMode === 'managed'`.
- Updated `src/features/admin/dashboard-sections.tsx` so admin listing rows now show `managed listing`, `managed host`, and managed settlement readiness badges directly in the main listing management table.
- Added `tests/ui/admin-listings-managed-hosting.test.tsx` to lock the visibility contract in place, then verified with `npm run test:ui -- tests/ui/admin-listings-managed-hosting.test.tsx` and `npm run lint:types`.
- Author signature: (|/) Klaasvaakie

# 2026-07-06 host subscription modal cancellation and plan-change flow

- Reworked subscription cancellation so it now schedules `cancel_at_period_end` instead of dropping the host immediately, and added a subscription expiry cycle that marks ended rows `expired`, downgrades the user back to `standard`, and deactivates paid billing access only after the paid window actually closes.
- Added host-facing billing endpoints for `/billing/subscriptions/:subscriptionId/cancel` and `/billing/subscriptions/:subscriptionId/change`, reusing the existing Yoco payment flow for both upgrades and downgrades from inside the host subscriptions modal.
- Rebuilt `src/components/HostSubscriptionsDialog.tsx` so self-service hosts can see the active cycle, schedule end-of-period cancellation with confirmation, and switch between Standard, Professional, and Premium directly from the modal; managed-host accounts still show their managed-state explanation instead of fake self-service controls.
- Tightened paid-host state handling so expired paid accounts no longer present as actively subscribed just because `billingSource` is still `paid`, and updated billing timeline logic so inactive paid hosts are pushed toward choosing a plan instead of the voucher path.
- Verified with `npx tsx --test tests/payment-yoco-contracts.test.ts`, `npm run test:ui -- tests/ui/host-subscriptions-dialog.test.tsx tests/ui/admin-listings-managed-hosting.test.tsx`, `npm run lint:types`, and `npx tsc --noEmit -p encore/tsconfig.json`.
- Author signature: (|/) Klaasvaakie

# 2026-07-08 encore deploy retry with replacement auth token

- Confirmed the latest billing and admin changes were already pushed to `origin/main` in commit `7594009`, so the remaining work was only the Encore backend deploy.
- Traced Encore local auth storage to `C:\\Users\\wimpi\\AppData\\Roaming\\encore\\.auth_token`, confirmed the helper expects a JSON-encoded OAuth token config rather than raw token text, and retried the deploy with the replacement token in structured config form.
- The auth parsing problem was cleared, but `git push encore main` still failed with `fatal: internal error. git-remote-encore: exit status 1`, which means the new token is still not accepted for access to the `encore://ideal-stay-online-gh5i` remote from this machine.
- No repo code changed in this pass; the deployment remains blocked on a valid Encore credential with access to the target app.
- Author signature: (|/) Klaasvaakie

# 2026-07-08 encore MCP and skills local setup

- Confirmed `.vscode/mcp.json` already contained the `encore-cloud` MCP server entry pointing at `https://api.encore.dev/mcp`, so no config patch was needed in the workspace.
- Installed the Encore skill pack with the current command path `cmd /c "npx skills add encoredev/skills"` after the deprecated `npx add-skill` wrapper failed to respawn `npx` correctly in PowerShell.
- The installer completed and copied 28 Encore skills into `C:\\Users\\wimpi\\Documents\\GitHub\\idealtrue\\.agents\\skills\\...`.
- The remaining step is still VS Code-side authentication for the `encore-cloud` MCP server; that flow is editor-owned and was not completed from the terminal.
- Author signature: (|/) Klaasvaakie

# 2026-07-08 encore.exe application control block diagnosis

- Confirmed the `encore` command resolves to `C:\\Users\\wimpi\\.encore\\bin\\encore.exe`, which sits in a user-profile path commonly blocked by AppLocker or WDAC path rules.
- Verified this is an OS application-control problem, not a repo problem: the command fails before project code runs, and no workspace change can override machine policy from user space.
- I could not change the policy from this session; the practical fix is to have admin allow the Encore binary by publisher, hash, or an allowed install path such as `C:\\Program Files\\Encore\\`.
- Author signature: (|/) Klaasvaakie

# 2026-07-08 Encore-managed payment webhook pipeline

- Used the installed Encore webhook and Pub/Sub skill guidance against the existing Yoco billing flow instead of inventing a second payment path.
- Kept the exposed Yoco webhook at `/billing/webhooks/yoco`, but changed it to verify the raw signature, persist the webhook event, publish the event id to an Encore Pub/Sub topic, and acknowledge immediately.
- Added `encore/billing/webhook-events.ts` and `encore/billing/subscriptions.ts` so webhook fulfilment now runs in a background Encore subscription via `processStoredYocoWebhookEvent(...)`, with the same ownership checks, status transitions, provider-order capture, and idempotent `processed_at` marking as before.
- Updated the payment contract tests to lock in the new fast-ack webhook pattern and verified with `npx tsc --noEmit -p encore/tsconfig.json` and `npx tsx --test tests/payment-yoco-contracts.test.ts`.
- The `encore-cloud` MCP server is configured in the workspace, but it was not exposed as a callable tool in this Codex session, so the implementation work was done directly in-repo using the installed Encore skills.
- Author signature: (|/) Klaasvaakie
2026-07-08 20:37:31 +02:00 - Adjusted `.gitignore` to ignore local `.agents/` and `skills-lock.json` artifacts; verified they no longer appear as untracked in `git status`.
# 2026-07-08 encore MCP auth attempt

- Confirmed `sani.md` already existed, verified `.vscode/mcp.json` already points `encore-cloud` at `https://api.encore.dev/mcp`, and confirmed `encore.app` is linked to app id `ideal-stay-online-gh5i`.
- Checked local Encore auth state: `C:\Users\wimpi\AppData\Roaming\encore\.auth_token` exists, but `encore auth whoami` returned a blank identity, so the stored token is not a trustworthy active session.
- Started a fresh `encore auth login` device flow from the terminal. The CLI produced pairing code `jaws-panda-jolt-spur` and opened the browser handoff URL `https://www.encore.dev/auth/device/fc0a677b-16a5-4aaf-8554-a8f17c098fd9`.
- The login process is still waiting for browser-side confirmation; terminal-side verification must resume after that handshake completes.

# 2026-07-08 encore-cloud tool exposure check

- Re-checked the live Codex tool surface after the auth handoff and `encore-cloud` still is not exposed in this session.
- That means the workspace config is not enough on its own; the Codex/VS Code side still needs a reload or a fresh session after authentication so the MCP server is actually attached.
- The next concrete step is to reopen or restart the Codex session in VS Code after the Encore auth flow finishes, then verify that `encore-cloud` tools appear before trying to push anything else.
- Author signature: (|/) Klaasvaakie

# 2026-07-09 GitHub-driven Encore deployment clarification

- Updated `docs/encore-deployment.md` to state the real GitHub-driven Encore path: GitHub pushes are the source of truth once the Encore app is connected to the repo and watching `main`.
- Kept the distinction explicit between GitHub/Vercel frontend deploys and Encore backend deploys so the deployment story is not misrepresented in the repo.
- No repo-local workflow was added because the actual Encore trigger lives in Encore Cloud connection settings, not in this worktree.
- Author signature: (|/) Klaasvaakie

# 2026-07-09 GitHub push deploy attempt

- Created commit `e7045ec` with the current repo changes and pushed `main` to GitHub `origin`.
- GitHub accepted the push to `https://github.com/WimpyvL/idealtrue.git`.
- If Encore Cloud is connected to the GitHub repo and watching `main`, that push is now the deploy trigger; if not, the missing piece is still the Encore-side connection, not the repo.
- Author signature: (|/) Klaasvaakie

# 2026-07-09 Encore browser auth recheck

- Opened the Encore device-auth URL in the in-app browser and verified it resolves to the Encore Cloud sign-in screen.
- The visible page is the login gate with GitHub, Google, email/password, and passkey options, so the browser is not yet past auth.
- Exact screen to use: `https://encore.dev/auth/device/fc0a677b-16a5-4aaf-8554-a8f17c098fd9`
- Author signature: (|/) Klaasvaakie

# 2026-07-09 Managed hosting payment reflection fix

- Fixed the paid-but-not-reflected Managed Hosting path by making `activateManagedHostingFromPaymentIntent` also create/update the visible premium monthly subscription row through `activatePlanFromBillingSession`.
- Routed Managed Hosting successful returns to `/host?modal=subscriptions` and updated the pricing-page reconciliation to keep the user inside the subscription modal after payment.
- Updated `HostSubscriptionsDialog` to refresh the profile on modal load so `managementMode = managed` is reflected immediately after checkout instead of depending on stale auth context.
- Verified with `npx tsc --noEmit -p encore\tsconfig.json`, `npx tsx --test tests\payment-yoco-contracts.test.ts tests\admin-financials-contract.test.ts`, `npm run test:ui -- tests\ui\host-subscriptions-dialog.test.tsx tests\ui\pricing-page.test.tsx`, and `npm run lint`.
- Author signature: (|/) Klaasvaakie

# 2026-07-09 package-wide billing reconciliation fix

- Added a shared pending billing payment reconciliation cycle so subscription, managed hosting, content credits, and host billing setup payments can still fulfil after the Yoco return/webhook timing window.
- Broadened Yoco paid status mapping to include `complete`, `success`, `approved`, `captured`, and `settled` across checkout and order lookups.
- Added an admin-only manual reconciliation endpoint at `/admin/billing/payments/reconcile` and a `pending-billing-payment-reconciliation` cron that checks pending provider-backed payment intents every five minutes.
- Verified with `npx tsc --noEmit -p encore\tsconfig.json`, `npx tsx --test tests\payment-yoco-contracts.test.ts tests\admin-financials-contract.test.ts`, and `npm run lint`.
- Author signature: (|/) Klaasvaakie

# 2026-07-09 deploy confirmation for billing reconciliation

- Confirmed the worktree was clean at commit `bc59fb2` (`Add pending billing payment reconciliation`).
- Re-ran `git push origin main`; GitHub reported `Everything up-to-date`.
- Re-ran `git push encore main`; the Encore deploy remote also reported `Everything up-to-date`.
- Author signature: (|/) Klaasvaakie

# 2026-07-09 payment account update root cause

- Ran a systematic debugging pass across the shared Yoco fulfilment path after package payments still failed to update accounts.
- Verified the live backend return route on `https://www.idealstay.co.za/api/encore/billing/payments/probe-payment-id/return?billingStatus=success` returns `303`, so the return endpoint itself is not dead.
- Found the cross-package root cause in webhook matching: `resolveYocoWebhookCheckoutId` only read `payload.metadata.checkoutId` and then fell back to `payload.id`, which can be the payment id rather than the checkout id. Real webhook shapes with `payload.checkoutId`, `payload.checkout_id`, or nested `payload.checkout.id` would not match `billing_payment_intents.provider_checkout_id`, leaving subscription, managed hosting, content credits, and host billing setup intents pending.
- Fixed the resolver to read metadata, direct checkout fields, and nested checkout fields before falling back.
- Verified with `npx tsc --noEmit -p encore\tsconfig.json`, `npx tsx --test tests\payment-yoco-contracts.test.ts`, `npx tsx --test tests\payment-yoco-contracts.test.ts tests\admin-financials-contract.test.ts tests\workflow-coverage.test.ts`, and `npm run lint`.
- Author signature: (|/) Klaasvaakie

# 2026-07-08 encore MCP status recheck

- Checked the current Codex tool surface again and there is still no exposed `encore-cloud` MCP namespace in this session.
- The blocker is not the repo config anymore; it is session attachment after Encore auth.
- The actionable fix remains the same: complete the Encore browser auth, then reload VS Code or start a new Codex session so the MCP server gets attached and the tools appear.
- Author signature: (|/) Klaasvaakie

# 2026-07-09 live payment 500 follow-up

- Treated the reported browser console errors as a live backend failure, not a completed fix: authenticated payment status polling and payment creation were still capable of returning Encore 500s.
- Hardened pending payment reconciliation so Yoco checkout/order lookup failures are logged and leave the payment pending instead of crashing the status endpoint.
- Expanded stored webhook reconciliation to match direct and nested checkout references, so already-received successful Yoco events can repair pending payment intents across all package types.
- Added retry handling around Yoco checkout creation, checkout lookup, and order lookup; exhausted provider/network failures now surface as provider unavailable instead of opaque internal errors.
- Verified with `npx tsc --noEmit -p encore\tsconfig.json`, `npx tsx --test tests\payment-yoco-contracts.test.ts tests\admin-financials-contract.test.ts`, and `npm run lint`.
- Author signature: ( |╲ ) Klaasvaakie

# 2026-07-09 payment status 500 architecture pass

- Re-mapped the real payment flow after live status polling still returned `500` for `/billing/payments/:paymentId?billingStatus=success`.
- Added `docs/payment-subscription-flow-map.md` to record the actual current flow, fracture lines, and the target rule that browser polling must not be the brittle activation point.
- Hardened `reconcilePendingPaymentIntent` so stored webhook lookup failures are logged and do not prevent checkout/order reconciliation from running.
- Hardened `getBillingPaymentStatus` so customer-facing polling returns the latest stored intent state if internal reconciliation throws, instead of surfacing an Encore internal error.
- Added regression coverage proving status polling fails closed and stored/provider reconciliation remains wired.
- Verified with `npx tsc --noEmit -p encore\tsconfig.json`, `npx tsx --test tests\payment-yoco-contracts.test.ts tests\admin-financials-contract.test.ts`, and `npm run lint`.
- Author signature: ( |╲ ) Klaasvaakie

# 2026-07-09 subscription lifecycle upgrade downgrade grace

- Added subscription lifecycle migration `14_subscription_lifecycle.up.sql` for pending downgrades, grace-period expiry, and notification marker fields.
- Changed self-service subscription upgrades to charge the rounded prorated unused-time difference, while downgrades are scheduled for the next billing date without checkout.
- Added a 7 day subscription grace period before deactivating paid host access, plus renewal due, grace started, and deactivated notifications.
- Updated the subscription modal to show pending changes and grace access, redirect only for upgrade checkout, and stay in-place for scheduled downgrades.
- Verified with `npx tsc --noEmit -p encore\tsconfig.json`, `npx tsx --test tests\payment-yoco-contracts.test.ts tests\admin-financials-contract.test.ts`, `npm run test:ui -- tests\ui\host-subscriptions-dialog.test.tsx`, `npm run lint`, and `npm run build`.
- Author signature: ( |╲ ) Klaasvaakie
- 2026-07-08: Traced the open dependency alert to a direct root dependency on `encore@^0.0.30-beta`, confirmed there are no runtime imports of the legacy `encore` npm package anywhere in this repo, and removed it with `npm uninstall encore`. That eliminated the exact vulnerable path `encore -> handlebars@1.2.1 -> uglify-js@2.3.6` from the root lockfile without touching the real backend package under `encore/`, which depends on `encore.dev` instead. Author signature: ( |╲ ) Klaasvaakie
- 2026-07-08: Verification exposed one unrelated-but-real break in `tests/ui/dashboard-mobile-shell.test.tsx`: the dialog mock was missing `DialogDescription` after `HostSubscriptionsDialog` started rendering it. Added that export to the test mock so the UI suite reflects the current component contract instead of failing on the mock seam. Author signature: ( |╲ ) Klaasvaakie
- 2026-07-08: Final verification after removing the vulnerable root `encore` package: `npm run build` passed, `npm run lint:types` passed, and `npm test` finished green with `213` unit tests and `54` UI tests passing. The only skipped step was the live smoke probe, which is intentionally gated behind `IDEAL_STAY_RUN_LIVE_SMOKE=true`. Author signature: ( |╲ ) Klaasvaakie

# 2026-07-08 payment return regression gate

- Added a focused contract test in `tests/payment-yoco-contracts.test.ts` to lock the successful checkout return path to `reconcilePendingPaymentIntent(...)` and `activatePlanFromBillingSession(...)`, so a regression back to "paid but no subscription" will fail the suite.
- Added a companion contract assertion for the `provider_order_id` reconciliation branch so webhook persistence and later order-based recovery stay wired together.
- Kept the change deterministic and repo-local by testing the billing source contract directly, with no external secrets or live Yoco dependency.
- Author signature: (|/) Klaasvaakie

# 2026-07-09 subscription plan state reconciliation

- Fixed normal self-service subscription fulfilment so it resets stale managed-hosting profile state by writing management_mode self_service when a plan payment activates.
- Tightened the subscription modal so a stale managed flag cannot override a conflicting active self-service subscription row; it now shows the active plan and a stale-state warning.
- Added regression coverage for the backend activation seam and the modal screenshot case where Professional was active while the profile still claimed Managed Hosting.
- Verified with encore typecheck, billing/admin contracts, focused modal UI test, lint, and production build.
- Author signature: ( |╲ ) Klaasvaakie

# 2026-07-09 managed downgrade state sync

- Fixed the scheduled-downgrade expiry branch so when Managed Hosting moves down to a self-service plan at the next billing date, the subscription row, user host plan, user management mode, and host billing account all move together.
- Hardened cancellation and grace expiry fallbacks so they clear `management_mode` to `self_service` even if `host_plan` was already Standard.
- Added a contract test proving effective scheduled downgrades clear managed mode and resync paid billing account state.
- Verified with Encore typecheck, billing/admin contracts, focused subscription modal UI test, lint, and production build.
- Author signature: ( |╲ ) Klaasvaakie

# 2026-07-09 premium versus managed modal distinction

- Fixed the subscription modal so Premium self-service at R499 is not classified as Managed Hosting just because stale profile/account state still says managed.
- Managed Hosting display now requires the premium monthly subscription to match the managed-hosting R650 amount in addition to the managed profile and active paid billing account.
- Added a regression test for the screenshot case: active Premium R499 with stale managed state must show Premium controls, not Managed Hosting.
- Verified with Encore typecheck, billing/admin contracts, focused subscription modal UI test, lint, and production build.
- Author signature: ( |╲ ) Klaasvaakie
